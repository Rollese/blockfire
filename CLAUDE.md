# Blockfire

**Agent instructions live in [`AGENTS.md`](AGENTS.md) — read it first.** It is the working agreement (graphify-first exploration, superpowers skills, hard gates, determinism/authority discipline, and §11 "land your work"). This file is just a pointer so Claude Code surfaces it; `AGENTS.md` is authoritative.

Quick reminder — **explore by querying the knowledge graph, not by grepping/reading files blind**: `python3 -m graphify query "<question>"` (graph is in `graphify-out/`, gitignored). Code auto-refreshes via a `post-commit` hook; see `AGENTS.md` §1.
