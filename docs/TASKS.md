# Blockfire Task Board

Canonical source of truth for what's being worked on. Claim a task (set owner + `in-progress`) before starting. See `AGENTS.md` for the working agreement.

**Status legend:** `todo` · `in-progress` · `blocked` · `review` · `done`

> **New here? Read [`STATUS.md`](STATUS.md) first** for the one-screen current state.
> Detailed plans/sessions/reviews for shipped milestones now live in [`archive/`](archive) (history, not current state).

## Milestone index

> **🚧 VEHICLES DEFERRED — owner-directed 2026-07-05.** All **remaining and future vehicle work** is deferred to a **dedicated post-core milestone**. The core priority is, in order, **infantry combat → proper real maps → destruction** — get those complete and playtested first; vehicles are added later. The M5 **land-vehicle sim substrate already on master stays** (it's harmless, prediction-ready, and gate-proven) but receives **no further investment**. Everything vehicle-facing is folded into this deferral and must **not** be picked up until the infantry/maps/destruction core is signed off: **manual turret aim/fire**, **client vehicle riding/exit/enter polish**, **vehicle combat/bot AI**, and **[M10](milestones/M10-air-vehicles.md) air vehicles**. When the core is done, spin these into one consolidated **"Vehicles" milestone** (fold M10 into it) rather than reopening them piecemeal. **Vehicle *spawns* were also removed from every shipping map 2026-07-05** (`vehicle_spawns: []` in all `maps/*.json`; generators `tools/map_gen.py` + `tools/map_gen_suburb.py` keep the coords preserved-but-not-emitted for trivial restore) so **no vehicles appear in-match** until the milestone reopens — guarded by `map_def_test.gd::test_shipping_maps_have_no_vehicle_spawns`.

| # | Milestone | Status | Gate (must pass to close) |
|---|---|---|---|
| M0 | [Foundations & decisions](milestones/M0-foundations.md) | **done ✅** | Empty client connects to empty server via custom message layer; bot driver connects 1 bot. |
| M1 | [Netcode core](milestones/M1-netcode-core.md) | **done ✅** | Bot fleet sustains 128 connected pawns @ 30 Hz on one Linux host within CPU/bandwidth budget. Gate run 2026-06-13: 128 players, tick_mean=17.65ms (budget <33.3ms) — PASS. See evidence in milestone doc. |
| M2 | [Core FPS loop](milestones/M2-core-fps-loop.md) | **done ✅** | Bots move + shoot each other; kills register; 128 bots stable. Gate run 2026-06-14: 128 players, peak-window tick_mean=30.01ms (budget <33.3ms), total kills=19 — PASS. See evidence in milestone doc. |
| M3 | [Conquest + respawn + squads](milestones/M3-conquest-squads.md) | **done ✅** | Bot-only Conquest match runs start→win at 128. Gate 2026-06-15 on separate-host fleet (unraid W-2275, server pinned, bots in Docker): `winner=0 elapsed=289s cap_events=7 peak tick=28.62ms<33.3` — PASS. Required a snapshot-cost fix (send staggering + enemy-prioritized relevance cap; `_send_snapshots` was 88% of the tick, O(N²)) — peak 95→28.6ms. 48-bot laptop gate also PASS (19.7ms). 88 unit tests green. See milestone doc. |
| M4 | [Building & destruction](milestones/M4-building-destruction.md) | **done ✅** | Phase 1 (Building) + Phase 2 (Destruction) both gate PASS 2026-06-15. Phase-2 fleet-128 (2/2 reruns): `winner valid elapsed=233/272s destroyed=5/23 nades=88/55 smoke=128 peak tick=29.48ms<33.3` — PASS. Destruction per-tick cost ~0.1ms (respawn phase); snap remains the dominant pre-existing cost. 140 unit tests green. See milestone doc. |
| M4.5 | [Combat depth & class identity](milestones/M4.5-combat-depth.md) | **done ✅** | **All three phases gated PASS on `game2`.** P1 (DBNO/revive/bandages) 2026-06-15; P2 (gadgets/RPG/penetration/attachments) 2026-06-16; P3 (ladders/vaulting/drop-shoot) 2026-06-16 (`winner=1 climbs=9 vaults=16 peak tick=23.46ms<33.3`). Body dragging deferred to M7 (per spec). 245 unit tests green. |
| M5 | [Vehicles — Land](milestones/M5-vehicles.md) | **done ✅ (Land)** | **Land Vehicles + Substrate CLOSED 2026-06-16** — fleet gate PASS on `game2` (`peak tick=23.67ms<33.3`, transport_m=930.8, enters=6; combat chain proven deterministically in `tests/vehicle_gate_test.gd`), `docker/srvlog-20260616-210141.log`. 309 unit tests green. **Air vehicles deferred → M10** (last; need the rendered client to tune flight/balance — owner-directed 2026-06-16). |
| M5.5 | [Combat Depth II (ballistics/loadout/suppression/melee/throwables)](milestones/M5.5-combat-depth-2.md) | **done ✅ (P1+P2+P3)** | **P1 (ballistics/fire-mode/secondary) CLOSED 2026-06-23** — fleet gate PASS (`peak tick=24.71ms`, proj=207 swaps=32; `docker/srvlog-20260623-124223.log`). **P2 (armor class + suppression) CLOSED 2026-06-23** — fleet gate PASS on game2 (`winner=0 elapsed=307s<600 cap_events=4 peak tick=26.91ms<33.3 supp=1323 agg=15.6Mbit/s`; `docker/srvlog-p2fleet-20260623-155605.log`). **P3 (melee/back-stab/sledge/flashbang/impact) CLOSED 2026-06-24** — fleet gate PASS on game2 (`winner=0 t1=0 elapsed=302s<900 cap_events=4 peak tick=24.62ms<33.3 agg=15.6Mbit/s melees=7 backstabs=3 sledge=8 flashes=3 flashblinds=44 impacts=6`; `docker/srvlog-m5.5-p3-20260624-090158.log`); 713 unit tests green; mechanics proven deterministically (`tests/melee_test.gd`, `tests/grenade_test.gd`, `tests/grenade_gate_test.gd`). **M5.5 complete.** Presentation deferrals (death-recap, tracers, suppression blur, flashbang white-out) live in **M7**. **Ballistics (P1) feeds M7-C2 prediction** (owner-directed 2026-06-17). |
| M7 | [Art pass + UX polish — rendered client](milestones/M7-art-ux.md) | **done ✅** | First human-playable rendered client. **Re-scoped 2026-06-16:** P1 playable client + HUD → P2 art kit + LOD. **Steam/VAC + anti-cheat L3 deferred** to a later online/anti-cheat track. **GATE PASS 2026-07-05** — owner end-to-end human playtest on `conquest_town` (laptop 1080p; server + 24 bots on game2): full Conquest match start→win with **victory/defeat end screen**; every close-out item confirmed (climbing lockout + strafe-off, grenade wall-bounce, ladder-on-collapse removal, empty-sprint hysteresis, recoil/spread, resolution-independent capture banner, match-end screen, explosion audio, 65535-recap fix). Session logs `docs/sessions/2026-07-05-m7-playtest-round6.md`. **Deferred out of M7 (own tracks):** reserve-ammo economy, killfeed (off by choice), map design + bot pathfinding, destruction fidelity, scoreboard stats, and B4/tick-lead netcode (`docs/specs/netcode-tick-lead.md`). |
| M6 | [Voice (proximity + squad)](milestones/M6-voice.md) | **in-progress** (logic core + codec merged) | Spec/ADR-0006/plan + pure logic core + Opus GDExtension **merged to master 2026-06-18** (suite 532/0; codec loads in Godot 4.6.3). **Remaining + gate blocked by M7 client**: relay-thread/`server_main` wiring, client capture/playback, `client_main` integration, human-validated playtest (voice works in a live match without breaking tick budget). |
| M7.5 | [Bot intelligence (tactical AI)](milestones/M7.5-bot-intelligence.md) | **P2+P3 done ✅; P4 + visual sign-off remain** | Tactical, human-like, fair-play infantry bots. **P2 (headless combat-IQ engine: perception/utility/humanize/behaviours in `bots/ai/*`, reflex brain retired) merged + 128-bot fleet-gated PASS 2026-07-01** on game2 (`docker/run-m7.5-gate.sh`, dense `conquest_town`: `winner=0 elapsed=108s peak tick=29.70ms<33.3 agg=20.6Mbit/s kills=4 shots=204 downed=5 revives=4`, bot-perf scales to 128; `docker/srvlog-m7.5-20260701-230709.log`). **Finding:** combat density is map-dependent/stochastic — sparse `proving_grounds` can resolve by capture-bleed with ~0 combat (was the symmetric-map "flank/spread" gap — since fixed 2026-07-03, review batch 6; NOT a fire-path defect — pipeline verified). **P3 (support & survivability) done 2026-07-03** on branch `m7.5-p3-support-ai`: revive (medic-priority)/self-bandage/needs-driven bag deploy/bag-seek/grenade+mine avoidance as utility behaviours (`bots/ai/behaviors/support.gd`) + review-§E inert-feature fixes (weights/reaction-delay/`--ai-profile`/priority/`AI_TICK_EVERY`/memory-read/`climb_seek`) + `GADGET_LIST`/`GRENADE_FX` fan-out to bots (fair-play, no wire change); **drag-to-safety descoped** (no body-drag sim mechanic — needs its own sim+wire increment); blackboard resupply → P4; suite 1146/0; ≤48 smoke PASS; 128-bot fleet gate **PASS 2026-07-03** peak tick 16.83ms<33.3, revives=2 bags=5, `docs/gate-evidence/20260703-171922-m7.5-p3.txt` — detail in the milestone doc. **Remaining:** P1 free-fly (partial: F8 cam), P4 squad/strategy, and the real gate = operator visual free-cam sign-off (deferred post-M7; combat *feel* tuning belongs there, not blind). |
| M8 | [Hardening & ops](milestones/M8-hardening-ops.md) | **complete ✅ (except deferred SIGTERM)** | Documented one-command stress run spins server + 128 bots in Docker. **P1 done 2026-07-01**: `docker/stress.sh` (+ shared `docker/_gate_lib.sh`) → `STRESS GATE: PASS` (128p, peak tick 20.53ms<33.3, full match); runbooks `running-a-stress-test.md` + `reading-telemetry.md`; spec `docs/specs/m8-hardening-ops.md`. **P2 done 2026-07-01**: `docs/specs/telemetry.md` schema-of-record + `pktloss` (954/0); NDJSON sink deferred (YAGNI). **P3 done**: adaptive degradation 2026-07-01 (`server/degrade.gd`, no-regression fleet PASS 18.27ms); **config file + map rotation 2026-07-03** (`server/config.gd` CLI>file>default + persistent multi-match rotation loop; spec `docs/specs/server-ops.md`; suite **1080/0**, `ci/m8_p3_rotation_test.sh` ROTATION SMOKE PASS; 128-bot stress no-regression **PASS 2026-07-03** peak tick 16.74ms<33.3, `docs/gate-evidence/20260703-121729-stress.txt`). Only remaining: SIGTERM graceful shutdown (investigated 2026-07-01 → not feasible in pure headless GDScript, **deferred** — see milestone doc, commit `cacfef3`). |
| M12 | [Squad FOB, cooperative construction & class refit](milestones/M12-squad-fob-class-refit.md) | **COMPLETE ✅ (P1+P2+P3)** | **P3 — squad-leader FOB CLOSED 2026-06-29** (128-bot fleet gate PASS on game2: `winner=0 elapsed=256s peak tick=22.48ms<33.3 fobs_built=1 fobs_destroyed=1` — a squad FOB cooperatively shovelled to completion live, then destroyed in-match; across two runs built/disabled/destroyed all fired emergently; mechanics proven deterministically in `fob_functional_test`; `docker/srvlog-20260629-235035.log`): leader-only `PLACE_FOB`→FOB build site→M4-destructible bunker spawn (completed+not-destroyed+no-enemy-within-40m), `_reconcile_fobs` lifecycle, `FOB_LIST`/`FOB_BASE` replication for M7; squadmate-rally fallback kept; client FOB UI/ghost/bunker model deferred to M7. **P2** — cooperative shovel construction CLOSED 2026-06-28 (`built_large=1`; `docker/srvlog-20260628-225159.log`). **P1** — class refit CLOSED 2026-06-28 (`c4=15 mines=3`; `docker/srvlog-20260628-184043.log`). |
| M13 | [Assault game mode](milestones/M13-assault-mode.md) | planned (deferred) | **Sequence: after core mechanics + client solid (post-M7/M7.5/M8).** Asymmetric attack/defend: defenders own all CPs (no uncap), attackers an uncap + ~2× tickets; defenders bleed only once all CPs fall; attackers also win by holding all CPs with no living defender. Any-order, recapturable; reuses Conquest machinery + M12 FOB. Plan of record now; implemented later. Decision: [ADR-0007](adr/0007-battlebit-divergences.md). |
| M11 | [Destructible buildings](milestones/M11-destructible-buildings.md) | **sim + feel done ✅ (Gate A PASS; Gate-B feel built + owner-playtested — final sign-off at the joint playtest w/ new buildings)** | BattleBit-style destructible map buildings on the M4 substrate. **P1 chunked-store unify + P2 support cascade/collapse + P3 building catalog/prefabs/art all merged to master 2026-06-18** (`shared/sim/support.gd`, `COLLAPSE` msg, `building_id` store, `buildings/*.json`, server cascade/collapse wiring). **Gate A (128-bot sim) PASS 2026-06-23** on game2 (`winner=1 elapsed=447s<900 cap_events=4 peak tick=24.65ms<33.3 destroyed=17 rstruct=25 collapsed=1, 0 errors`; `docker/srvlog-m11-20260623-191111.log`; scripts `ci/m11_buildings_test.sh` + `docker/run-m11-gate.sh`). **Remaining: Gate B (feel) + P4 client cosmetics — blocked by M7 client.** Map-scale note: **RESOLVED 2026-07-01** — the dense-map snapshot cost was the delta encoder, fixed by `EntityState.bake()` (encode −52%); `conquest_town` (8324 pieces) now PASSES the 128-bot gate at 27.31 ms (see the follow-up row below). Coordinates with M5.5-P3 (melee). |
| M14 | [Walkable multi-floor structures](milestones/M14-walkable-multifloor.md) | **merged to master ✅ (gates pending)** | Walkable multi-floor buildings: stairs (`shared/sim/stairs.gd`), per-floor walkability, fall damage (`shared/sim/fall.gd`). Code merged to master 2026-06-21; milestone doc gate rows (128-fleet re-confirm + human playtest) remain unresolved — was invisible from this board until 2026-07-02. |
| M15 | [Heightmap terrain](specs/heightmap-terrain.md) | **done ✅** | Heightmap terrain (hills/valley/too-steep ridge + tunnel-via-cutout): walkable, real bullet/LOS cover, driveable, composes with buildings; server-authoritative, client-mirrored, deterministic, **no wire change**. Pure `Terrain`/`TerrainGrid` query module (bilinear `height_at`, central-diff `slope_at`, `resolve_movement`, `load_for_map` w/ building auto-flatten pads + cutouts); folded into M14 floor chain, pawn/vehicle clamps, `StructureStore.march` occlusion, chunked client mesh; `tools/map_gen.py` heightmap generator → retrofitted `conquest_proving_grounds`. **Deterministic suite 1326/0**; **128-bot fleet gate PASS** on game2 (`winner=1 elapsed=249s cap_events=5 peak tick=16.72ms<33.3 collapsed=1, 0 errors`; `docker/srvlog-m11-20260706-*.log`). A final adversarial review caught sub-zero-terrain bugs (valley clamp / grounded-over-valley / vehicle self-block) + a first-gate fleet-freeze (hard pad-edge cliffs) — both fixed and re-gated. **Owner feel-gate PASS 2026-07-06** — `conquest_town` reworked into the playable M15 village (countryside heightmap: gentle rolling hills + smooth graded roads, procedural tree/rock scenery, BattleBit capture markers + HUD polish), owner-confirmed live on the desktop client. The last blocker (roads reading "wavy") was resolved by a **rolling-terrain + consistent-gradient road regrade** in `gen_town_heightmap` (wide-Gaussian road-corridor smoothing keeps the long swell, sheds the short grassy rolls onto grass banks → roads keep real elevation at a smooth consistent grade; master `7859727`). Hills-as-cover / no floating buildings / walkable slopes all confirmed. Non-blocking follow-up: multi-cell cutout footprint (I4). Plan/session: `docs/superpowers/plans/2026-07-06-m15-heightmap-terrain.md`, `docs/sessions/2026-07-06-m15-heightmap-terrain.md`, `docs/sessions/2026-07-06-wavy-roads-handoff.md`. |
| M16 | [Standing-bleed + bandage](archive/superpowers-specs/2026-07-03-standing-bleed-bandage-design.md) | **sim done ✅; 128-bot gate PASS (client feel pending)** | BattleBit standing-bleed survivability loop: a below-60-HP bullet/blast hit starts a bleed draining the standing `health` pool (~5 HP/s); ends only via a timed all-or-nothing bandage channel (self/teammate, 5 s / medic 2.5 s, heals 25 HP), else drains to 0 and routes into the DBNO halving-bleedout. Medic bandages 2× faster + carries 20 (was 5); the bandage item finally has a consumer and **revive now spends one** (victim-first→helper) so dry bots stop revive-looping. Impl 2026-07-09 (master, branch `m16-standing-bleed-bandage`): pure `shared/sim/bleed.gd`+`bandage.gd`, pawn bleed fields, `_apply_pawn_damage` trigger + `ServerSupport.step_bleed`/`step_bandage`/`_bleed_out_standing` + revive-costs-a-bandage, ally-only `BLEEDING_LIST` (47) + `BANDAGE_ACTION` (46) + SELF_STATE `bleeding`/`bandage_progress` (**`Protocol.VERSION` 4→5**), bot self-bandage, client F-hold bandage + reused cast-bar. Suite **1437/0**; connect smoke PASS; **128-bot fleet gate PASS** on `conquest_town` (`winner=1 elapsed=158s peak tick=23.83ms<33.3 bleeds=12 bandages=2 bleeddowns=3 revives=5`; `docs/gate-evidence/20260709-234921-m16-bleed2.txt`). Session `docs/sessions/2026-07-09-m16-standing-bleed-bandage.md`. **Deferred:** client visual *feel* (bleeding vignette / blood VFX / final cast-bar art — owner playtest); bot *teammate*-bandage (bots can't see a mate's bleed — self-bandage exercises the gate). |

| M17 | [Reserve-ammo economy](specs/reserve-ammo.md) | **done ✅ (sim + wire; client HUD feel pending)** | BattleBit finite spare-ammo: reload was a free mag-refill (infinite ammo); now each hit-scan weapon carries a finite `reserve_ammo` pool (AR 180 / SMG 210 / DMR 140 / PISTOL 60 ≈ 6 spare mags) and a reload moves `min(mag_size-mag, reserve)` rounds in — **no partial-mag discard** (shared `Weapon.reload_fill`, used by both the server's authoritative reload-complete and the client `WeaponPredictor` so the HUD never drifts). Reload is blocked at reserve 0; ammo crates / medic `give_ammo` / ammo bags refill the reserve; respawn/deploy/slot-swap reset it to full (6 mags/life → bots rarely run dry before dying, so fleet-gate combat density stays unchanged — **128-bot gate PASS** on `conquest_town`: `winner=1 elapsed=78s peak tick=24.27ms<33.3 kills=25 players=128, 0 script errors`, `docs/gate-evidence/20260710-003052-m17-reserve-ammo.txt`). Wire: SELF_STATE gains a trailing `reserve` u16, `Protocol.VERSION` **5→6**. HUD reads `"%d / %d"` (was `"%d /∞"`). RPG rocket pool out of scope (gadget-managed). Impl 2026-07-10 (branch `m17-reserve-ammo`): `shared/sim/weapon.gd` (`reserve_ammo`/`reload_fill`), `server/fire.gd` reload-start guard, `server_main.gd` reload-complete + inits/resets + ammo-box refill + SELF_STATE send, `server/support.gd::give_ammo`, `client/weapon_predictor.gd` (`reserve`/`reconcile_reserve`), HUD. Suite **1452/0**; connect smoke PASS (bot handshakes at VERSION 6). **Deferred:** client HUD text/tint feel (owner playtest — the mechanic is fully headless-tested); finite RPG rockets (follow-up). |
| M18 | [Battle Royale game mode](milestones/M18-battle-royale.md) | planned (deferred) | **Sequence: after core mechanics + client solid (post-M7/M7.5/M8) — the first new content mode after Conquest/Assault.** ~128 players in **squads of 4** parachute onto a large map, **scavenge** all gear from the ground, fight inward as a **shrinking zone** forces contact until one squad remains. PUBG/BattleBit-flavored (no Fortnite instant-build); **rules variant over the shared sim, session-only, zero M9 dependency.** Reuses squads/destruction/DBNO-revive/armor/reserve-ammo/shovel-construction; new: `br.gd`/`zone.gd`/loot + ground-items/parachute-glide/beacon-redeploy. Plan of record now; implemented later. Decision: [ADR-0009](adr/0009-game-mode-strategy.md); spec [`battle-royale-mode`](specs/battle-royale-mode.md). |
| M19 | [Class Select & Player Loadouts](specs/class-select-loadout.md) | **COMPLETE ✅ (all class gadgets live — Grapple, the last, shipped 2026-07-13)** | Player-chosen, server-validated, session-persistent loadouts + class-select screen: pick class (incl. Engineer), primary archetype + attachments, **player-picked armor** (L/M/H → dmg-reduction 1.0/0.85/0.7, move-speed 1.2/1.0/0.8), grenade (frag/smoke/flash), and 1-of-3 gadgets per class. **Passive perks only** (centralized `class_traits` + self-documenting `trait_blurbs`). RPG→Engineer gadget; new Support-only **LMG**; Claymore removed (gadget design tenet: active-use/visible-counterplay only). `primary` = weapon-variant id, archetype-gated via `Weapon.archetype_of`. **P1a** (shared/sim data model): DONE `085c721`. **P1b-1** (variant seam flip onto the landed weapon-variants registry): DONE `72b4c28`. **P1b-2** (sim+wire framework): DONE — `SET_LOADOUT`=48 (VERSION 7→8), per-connection persistence, `_apply_loadout_to_client` re-derives weapon/armor/class/gadget/rockets from the sanitized loadout on every spawn (HELLO/deploy/respawn), RPG gadget-only, `Loadout.bot_gadget`/`bot_loadout(id)` unified bot path, 6 server-integration tests. Suite **1542/0**; 128-bot conquest_town fleet gate **PASS** (tick 15.8ms<<33.3, script_errors=0; `cap_events≥1` variance-prone at 80 tickets). **P2a** (class-traits wiring): DONE — Engineer **+20% blast radius** (`blast_mult` in `_blast_at`, covers C4/RPG/frag), Support **extra reserve ammo** (`reserve_mult` at every spawn+resupply cap), **finite grenade pool** (Support 5 / others 3, refills on spawn), Assault **Combat Vigor** = new out-of-combat health regen (all classes regen; Assault faster) which **skips bleeding pawns** so M16 standing-bleed still downs, and LMG **suppresses harder** (`Weapon.suppression_mult` into accrual). Server-authoritative (health not client-predicted); 6 new/edited test files. Suite **1561/0**; 128-bot conquest_town fleet gate **PASS** (tick 16.66ms<<33.3, cap_events=3, script_errors=0). **P2b-structure** (cheap-reuse structure gadgets): DONE — **BREACH** (Assault): active placed charge, aim-at-structure → `_place_breach` (gated on selected gadget, rejects terrain hits) → arms `arm_delay_ticks` → `_step_breaches` carves a walkable hole via `_damage_structure(SRC_EXPLOSIVE)` + `_broadcast_detonation` FX; new `GA_BREACH_PLACE`=9 (no VERSION bump). **REPAIR** (Engineer): `step_repairs` gated on selected gadget + a structure branch (`aimed_damaged_structure` march → `_store.repair_chunks`, vehicle target still wins). Both in `IMPLEMENTED_GADGETS`; bot matrix + restrained bot AI (`bots/exercisers.gd`: REPAIR latch always-safe, BREACH capped 1/bot + 60s CD + path-block check) exercise both under the gate; client emits the actions off `default_gadget` (plumbing for P3). Read-only sim review fixed a vacuous negative test + terrain-hit gap. Suite **1572/0**; 128-bot conquest_town fleet gate **PASS** (winner=0, tick 16.82ms<<33.3, cap_events=3, script_errors=0). **P2b-medic** (Medic gadgets): DONE — **STIM** (Combat Stim): finite refillable charges (`stim_charges`, catalog 3/refill 3, per-use cooldown) → `GA_STIM_USE`/`_use_stim` sets `pawn.stim_until_tick`; timed buff = **+15% move speed + 2× stamina regen** (via a `cmd["stimmed"]` flag injected in `_step_movement` [server] + `_produce_input_frame` [client predicted], so `Pawn.step` applies the identical rule both sides), **suppression immunity** (`_step_suppression_decay` zeroes while stimmed), and **damage resist 0.8** (in `_apply_pawn_damage` after armor, before HP loss); refills from ammo resupply (`give_ammo`). **SMOKE_WALL** (Medic): `_place_smoke_wall` places a bigger (r=9)/longer (20s) smoke zone on a 15s cooldown, reusing `SMOKE_DEPLOYED`. Wire **VERSION 8→9** (`GA_STIM_USE`=10/`GA_SMOKE_WALL_PLACE`=11 + SELF_STATE `stim_charges`/`stim_ticks` append). Bot matrix rotates Medic HEAL/STIM/SMOKE_WALL + restrained bot AI. Read-only sim review fixed a hot-path per-tick alloc (dup cmd only when stimmed), a respawn cooldown-reset gap, + 2 tests. Suite **1605/0**; 128-bot conquest_town fleet gate **PASS** (winner=1, tick 16.35ms<<33.3, cap_events=1, script_errors=0). **P3** (client class-select / deploy loadout screen): DONE — **Task 1** (`9969919`): `client_main` stores `_loadout`, seeds `Loadout.default_loadout` on WELCOME, sends `SET_LOADOUT` on join, routes gadget input off `_loadout["gadget"]` (the seam that makes every P2 gadget reachable by humans). **Task 2** (`ClassSelectPanel`, `client/menus/class_select_panel.gd`): data-driven pickers class/primary(+attachments)/armor(L/M/H trade-off text from `Armor`)/grenade/gadget — all lists from `Loadout`/`Weapon`/`Armor` (no duplicated tables), unbuilt gadgets greyed "(coming soon)", every edit runs `Loadout.sanitize` and emits a deep-copied sanitized cfg. **Task 3**: perk panel from `trait_blurbs` (no hardcoded strings) + panel→`deploy_menu`→`client_main._on_loadout_changed`→`_send_loadout` (re-sends SET_LOADOUT on every live edit; seeded silently, connect-once, dim backdrop overlay). Two read-only reviews per task fixed a base-class name shadow, an overlay-stuck-visible state bug, + added deep-copy-independence & perk-populate tests. **Task 4**: real-GPU (game2 Xvfb, `tools/render_classselect_shots.gd`) 1080p screenshot per class delivered for owner sign-off; "None" attachment label polish. Client-only (no server/sim/wire change — SET_LOADOUT store+apply already gate-proven), so no new fleet gate. Suite **1611/0**. **P4** (LMG Nest): DONE — deployable/manned/destructible MG emplacement (`EMPLACEMENT_ACTION`/`EMPLACEMENT_LIST`/`GA_LMG_DEPLOY`, VERSION 9→10); 128p gate PASS. **P5** (Riot Shield, Support): DONE — carried directional bullet-block: a held `BTN_SHIELD` (=1024) raises a 300-HP shield that absorbs frontal (±75° half-arc) **GUN** small-arms into a server-owned pool at the cost of move speed (0.7×) + primary fire (shared `Pawn.step` rule, client-predicted via `fire_suppressed_by_shield`); flank/explosive/melee/back-stab all bypass (`RiotShield.is_small_arms` + `is_melee` guard); regen after ~3 s no-hit, a break arms a ~5 s lockout then re-arms full; `pawn.shield_up` derived server-side each tick (anti-exploit: never while downed/mounted-nest/in-vehicle); `SELF_STATE` gains a trailing `shield_hp_frac` byte (**VERSION 10→11**, no SNAPSHOT change). Pure `shared/sim/riot_shield.gd`; server block in `_apply_pawn_damage` + `_step_shield_regen`; client toggle→held-bit + HUD bar + class-select un-grey; support-bot exerciser (1/3 of Support bots). Fully TDD, per-task two-stage reviewed + final holistic review. Suite **1711/0**; **128-bot conquest_town fleet gate PASS** (winner=1, tick_mean 7.64ms<<33.3, script_errors=0, **shield_blk=238 shield_brk=12**, nests_dep=13 unregressed; `docs/gate-evidence/20260712-171828-m19-p5-riot-shield.txt`). **Grapple (Assault)** DONE ✅ (2026-07-13, closes M19) — a fired hook deploys a persistent, **vertical, climbable rope-ladder** anyone (friend/foe) can climb, reusing the existing `Ladder` system (`SimLoop.deployed_ladders` + `capture_ladder`, static-first). **Single charge** per life (restocked only from support `give_ammo`), **no lifespan timer** (persists through owner death; removed only on redeploy / disconnect / anchor-building collapse / cut). **1-rope-per-owner** across lives (owner-keyed eviction, `ServerDeployedLadders`). **Cut-the-rope**: after a 30 s arm delay (`CUT_ARM_TICKS=900`) any nearby player of any team severs it (`CUT_LADDER`, server-validated arm+radius+alive), removing it for everyone. **No firing while climbing, ever** (the existing `p.climbing` fire gate). Wire **VERSION 11→12** (`DEPLOYED_LADDER_LIST`=51, `CUT_LADDER`=52, `GA_GRAPPLE_FIRE`=13 reusing the generic gadget-action codec, SELF_STATE trailing `grapple_charges` u8). Client-only **verlet physics rope** (`client/fx/grapple_rope.gd`) sways beside the climb line, perf-guarded (static line beyond 38 m); climb-marker + rope hang off the per-ladder render node (self-heals with the DEPLOYED_LADDER_LIST). Class-select un-greyed. **Companion fix:** `_on_peer_disconnected` now sweeps the leaver's LMG nest (`ServerEmplacement.remove_owner`) — a pre-existing disconnect leak (C4/mines were swept, emplacements weren't). Built fully TDD via spec→plan→subagent-driven execution (12 tasks, per-task two-stage review). Bot exerciser aims the hook at the nearest tall **building** (not the enemy — that sailed the ray over open ground: the root-cause of an initial all-zero gate) and any bot cuts armed ropes; climb driven by feeding ropes into the existing climb-seek/driller paths (server stays authoritative, no teleport). Suite **1756/0**; **128-bot conquest_town fleet gate PASS** (winner=1, tick_mean 13.56ms<<33.3, **grapples_dep=14 grapple_climbs=2 grapple_cuts=9**, nests_dep=14/shield_blk=215 unregressed; `docs/gate-evidence/20260713-150905-m19-grapple.txt`). Design [`2026-07-13-m19-grapple-design.md`](superpowers/specs/2026-07-13-m19-grapple-design.md) · plan [`2026-07-13-m19-grapple.md`](superpowers/plans/2026-07-13-m19-grapple.md). Grapple deferrals: bot climb/cut counts are liveness-variable run-to-run (ropes anchor at wall faces bots rarely revisit — analogous to the gate's `cap_events` variance); ladder-less maps skip driller-forced rope climbs (climb-seek still steers). **M19 deferrals (whole milestone):** sidearm-behind-shield (v1 blocks all primary fire), remote-client shield render (visual feel — owner playtest), STIM teammate-inject (self-only v1), STIM HUD charges readout, grenade-type consumed at throw, client RPG viewmodel/HUD rewire, ammo-bag rocket resupply. Plans: [P1b-1](plans/2026-07-11-class-loadout-p1b-variants.md) · [P1b-2](plans/2026-07-11-class-loadout-p1b2-wire-server.md) · [P2a](plans/2026-07-11-class-loadout-p2a-traits.md) · [P2b-structure](plans/2026-07-11-class-loadout-p2b-structure.md) · [P2b-medic](plans/2026-07-11-class-loadout-p2b-medic.md) · [P3-client-screen](plans/2026-07-12-class-loadout-p3-client-screen.md) · [P5-riot-shield](plans/2026-07-12-riot-shield.md) (design [`riot-shield`](superpowers/specs/2026-07-12-riot-shield-design.md)). |

### Deferred — Beta / post-1.0 (not considered until every other milestone is finished)

> Per [ADR-0007](adr/0007-battlebit-divergences.md) §4 (2026-06-18): **M9 and M10 are deferred out of the core sequence.** The core deliverable is a complete, playtested LAN game (the M7 client + the gated sim systems incl. M12/M13). Do not start these until all other milestones are done.

| # | Milestone | Status | Gate (must pass to close) |
|---|---|---|---|
| M9 | [Online services (accounts, anti-cheat detection, matchmaking)](milestones/M9-online-services.md) | **deferred (beta)** | Steam auth → skill-tier placement → matched into an official 128-slot server (with dynamic tier-merge); signed match reports update rating; a seeded cheat trace is flagged. **Client-side release hardening specced 2026-07-11:** [ADR-0010](adr/0010-client-packaging-drm-hardening.md) + [`client-packaging-release-hardening`](specs/client-packaging-release-hardening.md) — server-side Steam **ownership** validation (Goldberg-emulator resilient), export hygiene (no secrets/server-only code in the client `.pck`), DTLS transport, wire/DoS hardening, PII/90-day retention. Complements [ADR-0004](adr/0004-anti-cheat-and-skill-matchmaking.md). |
| M10 | [Air vehicles (final content pass)](milestones/M10-air-vehicles.md) | **deferred (beta, last)** | Was M5-P2. Helicopter/jet on the M5 vehicle substrate. **Blocked until the M7 rendered client** — flight feel + heli/jet balance must be tuned visually by playtest, not blind off telemetry (owner-directed 2026-06-16; see AGENTS.md §10). |

> Milestones are **sequenced and gated**. Do not start a milestone before the previous gate passes. M4–M6 may be reordered but each remains independently gated. **As of 2026-06-16: the rendered client (M7) is pulled before M6** (voice's gate needs human testers in a live match → needs the client), and **air vehicles are deferred to M10 (last)** — they need the client to balance by feel.
>
> **As of 2026-06-18 ([ADR-0007](adr/0007-battlebit-divergences.md)): two new milestones + a beta deferral.** **M12 (squad FOB & class refit)** is near-term (before/with M7 so the client builds the right class/spawn UI): removes Recon, makes the DMR an Assault-only marksman with no sniper rifles, moves the claymore to Engineer, and replaces free squad spawn with a leader-built FOB (squadmate-rally kept as fallback). **M13 (Assault mode)** is plan-of-record, deferred until the core + client are solid. **M9 + M10 are deferred to a beta / post-1.0 phase** — not considered until every other milestone is finished.
>
> **As of 2026-06-17: M5.5 (Combat Depth II) added** from the BattleBit feature-gap review. It is sim-layer and bot-gated like M4.5, but its **ballistics model is an input to the in-flight M7-C2** (bullet drop changes client prediction — decided now so M7 builds projectile-aware, not hit-scan). The remaining M5.5 phases follow the M4.5→M7 pattern (prove mechanics + budget headlessly; tune feel on the visual client). Two accepted gap-review items are presentation and live in **M7** instead: the **death-recap card (M7-P1)** and **audio (M7-P2)**.
>
> **As of 2026-06-18: M11 (Destructible Buildings) added** (owner-directed — "destruction + gunplay feel are the most important parts of the game"). BattleBit-style destructible map buildings on the M4 substrate: a unified chunked `StructureStore` (0.25 m / 64-bit chunk masks), support-reachability collapse cascade, and a client cosmetic layer (holes/debris/collapse cinematic). **Spec authored now, build sequenced after the M7 client** (the feel gate needs rendering + playtest — same now-spec/later-build pattern as M5/M10 vehicles). Unifying the store **re-gates M4**. Bullets do not damage building walls (explosive/melee only).

> **As of 2026-07-05: the M7-P1 playtest fixes (branch `playtest-fixes-m7-round2`) are MERGED to master (`70946b3`, pushed).** The owner iterated rounds 3–5 on the desktop and asked to merge so a concurrent agent can branch from master with the fixes included. Landed: melee/vault/jump/ADS/smoke fixes, a 33-finding global review, and round-4/5 fixes (disconnect crash, vehicle steering, **manual vault**, grenade wall-bounce, **C6 stamina reconcile**, stamina bar, empty-slot grenade, reconcile-deadzone widening). Suite 1256/0. Session log: **`docs/sessions/2026-07-05-m7-playtest-round4-5.md`** (SELF_STATE trailing-byte order, run-server-in-tmux). The full end-to-end M7-P1 playtest **gate sign-off is still open** (not all items re-tested; owner heading out mid-loop). **Follow-up 2026-07-05 (pm): B1/B2 red roof ladders DONE + MERGED to master (`1cf0bd2`, pushed)** — 33 exterior red ladders generated onto `conquest_town` (tall buildings, roof ≥8 m; `tools/map_gen.py`), the inert `_make_ladder` scaffolding is now wired (`world_renderer.setup` render loop), `tests/map_ladders_test.gd` proves every ladder top lands on a real walkable roof deck (suite **1259/0**), visually validated on game2 (Xvfb close-up). **Explicit tick-lead netcode now SPECCED** (not yet implemented — deliberately, per the owner's "do it with the fleet gate, not live trial-and-error"): **`docs/specs/netcode-tick-lead.md`** (adaptive input-buffer clock sync, `SELF_STATE` gains a `u8 input_buf_depth`, `VERSION` bump, control-law + loopback + fleet + feel gates). **Still deferred (not started):** the tick-lead *implementation*, manual vehicle turret.

> **2026-07-05 (late) — tick-lead netcode IMPLEMENTED** (`docs/specs/netcode-tick-lead.md` → status
> updated in-spec). `client/tick_lead.gd` control law (TARGET=2, GAIN=0.05, anti-windup PHASE_MAX=2)
> paces input production 0/1/2 frames per physics tick from the new trailing `input_buf_depth` byte on
> SELF_STATE (**`Protocol.VERSION` 3→4**, registry updated); server samples `input_buf.size()` right
> after the `_step_movement` drain. Owner-client only (deployed + menu closed + on foot; bots/seats
> untouched); phase resets on deploy. **Proof:** `tests/tick_lead_test.gd` — control-law units + a
> closed-loop jitter/drift sim (connection-phase offsets + clock skew; buffer-edge events 248→96,
> slow-drift 4.6x, fast-drift 2.0x); live loopback converges `lead_d=2` with exactly 2 catch-ups then
> zero adjusts; 128-bot conquest_town fleet gate **PASS** (peak tick 30.48 ms / 33.3 budget,
> `docs/gate-evidence/20260705-213626-ticklead.txt`). **Open:** owner feel-gate playtest (meter:
> `[client-dbg] lead_d/holds/extras`); deadzone narrowing 0.12→? only after that validates.

> **2026-07-10 — perf track: native snapshot encoder (ADR-0003 seed + Phase 0 measured).** Opened the
> ADR-0001 escalation gate. **Phase 0 (measurement-first) is decisive:** on a budget-CPU proxy (server
> pinned to 14900KS **E-cores**, bots on P-cores), the 128-bot `conquest_town` gate **FAILS** — peak
> tick 34.84 ms (p99 37.46) vs the 33.3 ms budget, with the `snap` (snapshot-encode) bucket = **24.0 ms
> (×1.72 vs 14.0 ms on P-cores), ~69% of the tick**. `snap` is GDScript-CPU-bound (scales with
> single-thread perf) and the `interest` query is a separate 0.2 ms bucket, so ~80–90% of `snap` is
> addressable by a native encoder. **Conclusion: the native escalation is REQUIRED for budget hardware,
> not optional.** Proposed decision: Rust `gdext` (voice_opus template), **two phases** — (A) native
> columnar state-extraction + encoder behind the byte-identical `Snapshot.encode` seam (projected `snap`
> → ~5–8 ms, restores budget at 128p); (B) parallel per-client send across a worker pool (the 256-player
> / multicore endgame; sim stays single-threaded for determinism). Seed for the deep-planning pass:
> `docs/adr/0003-native-snapshot-encoder.md`; evidence `docs/gate-evidence/20260710-163108-phase0-ecore-profile.txt`
> (the FAIL is intentional — it's the "before" that Phase A must flip). [[blockfire-native-snapshot-encoder]].

> **2026-07-10 (later) — ADR-0003 RATIFIED + implementation plan ready (planning pass done).**
> Brainstorm → design → plan completed: **ADR-0003 → Accepted** with all open questions decided
> (columnar schema w/ GDScript-side quantization; native-owned baselines as Arc-shared tick columns +
> per-client ordered sent-id history; `begin_tick`/`encode_for` FFI; parity = differential fuzz +
> golden vectors + `--parity-audit` fleet run; interest stays GDScript; Linux-x86_64-only build;
> committed target = **128p on budget hardware via Phase A**, Phase B parallel-encode designed but
> deferred until 256p is scheduled or destruction erodes the margin). Design:
> `docs/superpowers/specs/2026-07-10-native-snapshot-encoder-design.md`. **Build-session plan (TDD,
> 14 Phase A tasks + deferred Phase B): `docs/plans/2026-07-10-native-snapshot-encoder.md`** — hard
> checks are byte-parity suites + the E-core FAIL→PASS flip (`ENCODER=gd` vs native A/B).

> **2026-07-10 (evening) — ADR-0003 Phase A BUILT + GATES PASS (done ✅, same-day).** Executed the
> full 14-task plan in-session. `native/snapshot_encoder/` (Rust gdext 0.5.3, `NativeSnapshotEncoder`,
> release .so, Linux-only) + `shared/net/snapshot_columns.gd` + `server_main.gd` native path w/
> `--encoder=gd` fallback, ack/reenter/disconnect/rotation hooks, `--parity-audit`. **Evidence:
> E-core A/B flip `ENCODER=gd` FAIL 35.05ms → native PASS 28.87ms** (`snapenc` 13.4ms→0.46–0.9ms,
> ~15–29×); live parity audit **0 mismatches** over a 128-bot match; P-core no-regression **18.38ms**
> (was ~24–26ms); suite **1463/0**; CI green w/ new Rust build steps (cargo 11/11; parity tests
> fail-loud in CI). Task 1 sub-bucket instrumentation (`snapq/snapenc/snapsend/snapstruct/snapself`)
> landed first and re-measured the addressable share honestly (13.4ms, not the seeded ~20ms — `snapq`
> per-client interest queries are 5.8ms and stay GDScript). **Next perf levers if E-core margin ever
> needs more: `snapstruct` join-flood (12.3ms peak window) and `snapq` — both outside ADR-0003
> scope.** Phase B (parallel encode) stays deferred per the ADR trigger conditions. Servers need
> `cargo build --release --manifest-path native/snapshot_encoder/Cargo.toml` before fleet gates
> (stress.sh fail-fasts if the .so is missing; `ENCODER=gd` opts out).

> **2026-07-10 (night) — ADR-0003 A.5: native interest+cull + struct-scan memoization (done ✅).**
> Owner-ratified follow-up: interest membership + enemy cull moved INTO the native encoder
> (`encode_for_auto` — exact 3D distance on the tick's quantized columns, teammates-always,
> nearest-N enemies ties-by-id; wire record order = column order). **Parity contract deliberately
> relaxed for this layer to decoded-VIEW equality** (owner decision — decode is order-independent):
> new oracle harness `tests/native_interest_view_test.gd` (3 seeds × 120 ticks × 4 clients, exact
> view equality, cull hammered); the byte-level fuzz/golden suites still pin the codec core via
> explicit-interest `encode_for`; `--parity-audit` reworked to shadow-view comparison (field
> mismatch on common entities hard-fails; radius-boundary membership drift tallied, telemetry-
> reported). Plus `_sync_structure_baselines` memoization (skip the 81-region rescan unless grid
> cell / piece+site epoch / backlog changed; 1-in-32-sends backstop). **E-core gate: PASS 22.70ms
> (Phase A 28.87 → A.5 22.70; reference 35.05)** — `snapq` 6.8ms→~10µs, `snapstruct` steady
> 3.3ms→0.7–1.4ms, steady `snap` ~2.8ms. Suite 1464/0; live audit 16-bot run 0 mismatches, 0 drift.
> Remaining peak driver = join-time baseline flood (paced send volume) — the next lever if needed.

> **2026-07-10 (night) — FULL 30 Hz SNAPSHOTS (done ✅).** `SNAPSHOT_STRIDE` 2 → 1: every client
> now gets a snapshot + SELF_STATE every tick (was 15 Hz). The historical stride-2 is the FIRST
> degrade-ladder step (`Degrade.LEVELS` gained a `[1,24]` level 0), so a struggling host
> automatically falls back to the old behavior. **E-core gate: PASS 23.30ms** (vs 22.70 at 15 Hz —
> the native encoder makes the doubled sends nearly free: snapenc 0.9→1.7ms, snapself 0.7→1.3ms).
> **Bandwidth doubles: agg 24.2 → 47.3 Mbit/s at 128p (~370 kbit/s/client)** — packet rate
> dominates under constant movement; fine for dev/typical dedicated uplinks, lean on the degrade
> ladder or future send-rate LOD if a host is uplink-constrained. Suite 1464/0; no degrade steps
> during the gate. Evidence `docs/gate-evidence/20260710-185158-30hz-ecore-native.txt`. Client
> feel (smoother remotes, per-tick reconcile feedback) → owner playtest.

> **2026-07-10 — netcode: reliable BULK channel (anti-HOL).** Implements the 2026-07-03 architecture
> review §D3. All reliable server→client traffic shared ENet channel 0, so a dense-map
> structure-baseline flood (town = 8324 pieces) could head-of-line-block latency-critical SELF_STATE
> (ammo/reload/stamina, reliable every stride) — a measured ~170 ms p99 stall at 48 bots. Added
> `NetHost.CHANNEL_BULK` (3) and moved structure baselines/deltas/collapse/collapse-warning onto it;
> everything else stays on CONTROL (0). Receivers dispatch by message id and ignore the channel, so it's
> a pure transport-lane split (no message format change); `Protocol.VERSION` **6→7** so a
> mismatched-channel-count old client is rejected. Suite **1455/0**; connect smoke PASS (bot synced 113
> pieces over BULK); **128-bot gate PASS** on `conquest_town` (`winner=1 elapsed=71s peak tick=25.78ms<33.3
> kills=27 players=128, 0 script errors`; `docs/gate-evidence/20260710-003919-netcode-bulk-channel.txt`).
> [[blockfire-netcode-bulk-channel]]. Remaining review items (native snapshot encoder B1, instanced
> remote players B2, infantry lag-comp D1) are larger/owner-steerable — not done here.

> **2026-07-05 (pm) — M7 playtest round 6 (live, laptop .116).** Owner playtest → fixes landed on `m7-playtest-round6-fixes`, suite **1266/0**. Session log: `docs/sessions/2026-07-05-m7-playtest-round6.md`. **Fixed:** A1 no-combat-while-climbing (+ strafe-off dismount, swap kept), B3 bigger stamina bar, C3b the "65535 damage" recap overflow (credit `min(dmg, pre-hit HP)`), C4 client grenade wall-bounce (was tunneling), H1 ladders removed on building collapse, **H2 the ~1 Hz empty-sprint rubberband** (BattleBit sprint-lockout hysteresis; **`Protocol.VERSION` 2→3** for a SELF_STATE `sprint_locked` byte), C1a visible spread + a recovering visual camera-recoil kick. **Decisions:** killfeed stays OFF (BattleBit-minimal, [[blockfire-no-killfeed]]); map design / bot pathfinding / destruction-fidelity / scoreboard-stats deferred to their own milestones. **Deferred:** B4 jump-apex "second tiny jump" folded into the **tick-lead netcode** (`docs/specs/netcode-tick-lead.md`) — same feel-critical class, do it with the fleet gate.

> **2026-07-06 — collapse round (tick-lead already merged; live laptop playtest).** Landed on master (`948def0`, suite **1291/0**): **collapse lethality** (a building coming down crushes players/vehicles/deployed gear in its footprint + 1.5 m, anchored on the building's *original* footprint cached at map load — `shared/sim/collapse_zone.gd`), **silent rejoin rubble** (past collapses no longer re-detonate on connect), **far-ghost-ladder fix** (a building damaged to zero pieces now collapses → frees its ladders), **grounded rubble** (client `_building_footprint` union, not the surviving remnant), **viewmodel hidden when dead**, and `--fast-nades`/`--fast-rpg` made **humans-only**. Owner-confirmed live: small/edge/interior collapses all kill, rubble on the ground. **Two follow-ups deferred to their milestones (documented, not just noted here):** (1) **connect-time loading screen** to hide the initial structure-delta streaming → [`hud-ui.md`](specs/hud-ui.md) "Connecting / loading screen (deferred)"; (2) **bot RPG-at-structure aggression** balance (bots chew the map — ~815 rockets/28 min) → [`bot-ai.md`](specs/bot-ai.md) §8 "Known balance gap".

## M11 — Destructible Buildings — P1 done 2026-06-18 (unit-verified; no-regression vs master)

Spec: [`docs/specs/destructible-buildings.md`](specs/destructible-buildings.md). Milestone: [`M11-destructible-buildings.md`](milestones/M11-destructible-buildings.md). Owner-directed 2026-06-18 (destruction + gunplay are the game's most important feel). Ratified decisions: unify `StructureStore` onto a chunked model (+ M4 re-gate); 0.25 m / 8×8 / 64-bit chunk masks; support-reachability cascade degrading to whole-building collapse; per-type bullet immunity (A); MultiMesh holes + GPUParticles debris (B); sledge coordinated with M5.5-P3 (C); fully procedural art. **Build blocked by the M7 client** (cosmetic layer + feel gate).

| Task | Owner | Status | Notes |
|---|---|---|---|
| M11 brainstorm + spec | claude | done | `docs/specs/destructible-buildings.md` (this brainstorm-of-record); branch `m11-destructible-buildings` |
| M11-P1 plan (chunked store) | claude | done | [`2026-06-18-m11-p1-chunked-store.md`](archive/plans/2026-06-18-m11-p1-chunked-store.md). **Option B** full flip (chunk mask + spatial clear + per-type immunity + OP_CHUNK wire). Hole-aware march **descoped** to a later phase (chunk-face geometry needs the art); tasks merged where API removals forced test co-migration |
| **M11-P1 execute — chunked store (unify)** | claude | **done ✅** | Subagent-driven TDD (two-stage review per task). ChunkMask, catalog fields, chunked `StructureStore` + spatial `damage_chunks` + per-type immunity, `OP_CHUNK` wire, bot/client mirrors, server fire/blast rewire. Unit **461/0**; server boots clean; **no-regression vs master** (branch≡master match telemetry — see milestone evidence). Cascade/collapse is P2 |
| **M11-P2 execute — support cascade + collapse** | claude | **done ✅** | Merged to master 2026-06-18. `shared/sim/support.gd` (BFS orphan flood-fill + `should_collapse`), `building_id` store index, `Msg.COLLAPSE`, server `_resolve_cascades` (orphan removes or whole-building collapse > threshold). Tests: `support_test.gd`, `protocol_collapse_test.gd`, `server_buildings_functional_test.gd`. |
| **M11-P3 execute — building catalog/prefabs/art** | claude | **done ✅** | Merged to master 2026-06-18 (+ later building/art polish). `BuildingCatalog`, `buildings/*.json` prefabs, `MapDef.buildings[]` stamping, procedural `client/art/building_kit.gd`. Tests: `building_catalog_test.gd`, `map_buildings_test.gd`, `building_art_kit_test.gd`. |
| **M11 Gate A — 128-bot sim fleet** | claude | **done ✅** | PASS on game2 2026-06-23 (was deferred while AI inert; now AI fires). `conquest_proving_grounds` TICKETS=80: `winner=1 elapsed=447s<900 cap_events=4 peak tick=24.65ms<33.3 destroyed=17 rstruct=25 collapsed=1, 0 errors`; `docker/srvlog-m11-20260623-191111.log`. Scripts `ci/m11_buildings_test.sh` + `docker/run-m11-gate.sh` (compose plumbs `MAP`). Mechanic proven deterministically (`server_buildings_functional_test.gd`). |
| M11 execute — client cosmetic layer (P4) | claude | **in-progress** | First pass landed 2026-06-27 (merged to master): per-piece destruction debris/dust (`WorldView.take_struct_fx`), collapse cinematic (`_play_collapse_fx`: dust cloud + debris burst + camera shake + rubble mound), `--destroy-test`/`--collapse-test`; visual-validated on `.128`; 843/0. Superseded by the Gate-B pass below (hole-aware geometry now shipped). |
| **M11 Gate-B feel — hole-aware destruction** | claude | **in-progress** (round-2 impl landing; owner playtest ongoing) | Branch `m11-gate-b-feel`, merged→master + pushed. Owner chose **Full BattleBit walk-through holes** (2026-07-06). Harness `tools/render_destruct_shots.gd`. **Round 1** (`c71ccc8`, owner-playtested): Q-A footprint-scaled collapse; H1 hole-aware geometry (promote-on-damage per-chunk render, pristine stays batched); H2 hole-aware march (shoot-through, one bit-test/hit); H3 walk-through (`region_clear` floor breach). Gates PASS conquest_town. **Round 2 from owner playtest** (`ed9fdea` merged; R5 `53833a6` gating): **R1** floors no longer render as vertical blocks (only walls get sub-cell holes); **R2** balance — frag nicks (0.4m), RPG breaches (1.3m), C4 heavy (2.2m), no one-shot buildings; **R3** carved walls stop bearing load (`support_intact` 0.25) → upper floors collapse not float; **A1** irregular holes (deterministic rim jitter); **R5** collapse leaves walkable INDESTRUCTIBLE `brubble` remnants (real collision — no invisible-hiding). Suite 1360/0. **R4** (vault a carved-down stub) done. **Round-2 live playtests (2026-07-07)**: rocket detonates on the wall it hit (not past it), direction-independent + cell-spanning carve, no corner cross-carve, **E/W-wall hole mirror fixed** (ChunkMask U-axis/origin now match the renderer for all yaws); balance (frag 0.4/RPG 1.3/C4 2.2, support fraction 0.12); **collapse needs ~half the building orphaned** (COLLAPSE_FRACTION 0.5) so upstairs damage drops the local section not the whole building; BattleBit collapse (7s warning + longer cinematic + walkable indestructible rubble ruin across the original footprint); seamless triplanar holed walls (no dark tint); colour-matched tumbling brick debris; F8 free-fly no-fire. Suite 1372/0; gates PASS. **R6** (organic collapse) deferred. **Owner feel sign-off pending a joint playtest once 2 new asset-based buildings are integrated.** |
| M11 follow-up — dense-map snapshot cost | claude | **done ✅** | **conquest_town (8324 pc) now PASSES the 128-bot gate** on game2 (`winner=1 elapsed=189s peak tick=27.31ms<33.3 destroyed=16 struct=8324 agg=20.0Mbit/s, 0 errors`; `docker/srvlog-m11-20260701-160937.log`) — was FAIL 34.08ms. Fix was **algorithmic, not feel**: profiling showed the delta ENCODER dominated `snap` (not query/sort/LOD). `EntityState.bake()` quantizes each wire field once per fresh state (was ~3×/client-send); `_diff_mask`/`_put_fields` diff/write cached ints. Bit-identical output (roundtrip tests unchanged, 945/0). Bench: encode 11.79→5.66ms (−52%), snap total −40%. Branch `m11-snapshot-encode-bake`. The rate/precision-LOD feel levers stay unused (not needed). |
| Coordinate melee wall-damage with M5.5-P3 | claude | **done ✅** | Resolved by M5.5-P3 landing: the Engineer sledgehammer swing calls `_damage_structure(id, SRC_MELEE, impact, SLEDGE_STRUCT_RADIUS)` → `StructureStore.damage_chunks(..., SRC_MELEE, ...)` (server_main `_resolve_melee`); per-type `takes_damage(type, SRC_MELEE)` immunity honoured. Gated in the M5.5-P3 fleet run (`sledge=8`). |
| M15 village round 2 — conquest_town retrofit + HUD/gameplay polish | claude | **done ✅ (roads RESOLVED, `7859727`)** | Master `f5688d7` (pushed). `conquest_town` is now the DEFAULT playable village: gentle heightmap core + countryside hills (77→31 buildings), grass MultiMesh, **procedural** tree/rock materials (the GLBs are trimesh exports with no UVs — baked textures can't map), tree/rock scenery, road/sidewalk **SURFACE SPLATMAP** painted into the terrain shader. Same round (owner-approved): BattleBit **capture markers** (square/diamond + letter + distance + progress ring, hide-behind/edge-fade), **compass letters** (relative colour), **top-center score band** (tickets/timer/WINNING-LOSING), **ammo/med box glyphs + friendly radius rings**, **grenade terrain-ground fix** (was instant-explosion/underground-dud on sloped ground), match-over deploy fix. Suite **1335/0**; 128-bot gate PASS on `conquest_town` (~24 ms). **RESOLVED 2026-07-06 (`7859727`, owner feel-gate PASS):** the "wavy roads" were fixed by a rolling-terrain + consistent-gradient road regrade in `gen_town_heightmap` (wide-Gaussian road-corridor smoothing keeps the long swell, sheds short grassy rolls onto grass banks). Handoff history: **`docs/sessions/2026-07-06-wavy-roads-handoff.md`**. |

> **⚠ SUPERSEDED 2026-07-01:** the warning below applied while the combat AI was being rewritten (2026-06-18 → 2026-07-01) and bots were inert (`shots=0`). The M7.5 tactical-AI engine has since landed and fleet-gated at 128 bots (`kills=4 shots=204`, see the M7.5 row above) — **AI-based full-match gates are valid again.** Kept only to explain why M11-P1's re-gate was recorded as unit + parity evidence: **⚠ Do not gate on AI-based full-match tests right now (2026-06-18):** the **combat AI is being completely rewritten** (separate workstream). While that is in flight, full-match runs have **inert bots** — they connect (48/48) and move but never fire/build (`shots=0 bld=0 kills=0`, tickets stuck 80/80, no winner), no script errors — **identically on master `b0ff265`** (independent of any feature branch). So the `ci/m4_*` smokes and fleet matches can't verify combat/attrition until the AI rewrite lands. Use **unit/functionality tests** (`godot --headless --path . -- --test`) and branch-vs-master telemetry parity for no-regression. M11-P1's re-gate was therefore recorded as **unit 461/0 + branch≡master integration parity** (no regression), not a fresh smoke PASS — AI-dependent behaviour is verified later.

## M6 — Voice (Proximity + Squad) — logic core landed (2026-06-18)

Spec: [`docs/specs/voice.md`](specs/voice.md). ADR: [`0006-gdextension-voice-codec.md`](adr/0006-gdextension-voice-codec.md). Plan: [`2026-06-18-m6-voice.md`](archive/plans/2026-06-18-m6-voice.md). Milestone: [`M6-voice.md`](milestones/M6-voice.md). Built on isolated worktree branch `worktree-m6-voice` (subagent-driven, two-stage review per task). The **pure tested logic core** is complete + green; the **shells + integration** remain (real prerequisites below). Full suite **509 run / 0 failed**.

| Task | Owner | Status | Notes |
|---|---|---|---|
| M6 voice brainstorm + spec | claude | done | `docs/specs/voice.md` — relay-not-mix; 2nd-port + E-core relay thread (full tick isolation); Opus via first GDExtension; proximity PTT enemies-audible + squad PTT team-private. Owner-ratified forks 2026-06-18. |
| ADR-0006 (first GDExtension) | claude | done (Proposed) | `docs/adr/0006-gdextension-voice-codec.md` — stub; promote to Accepted when the codec lands. |
| M6 implementation plan | claude | done | 4 phases, TDD. P0 Opus gdext · P1 pure wire/routing/jitter/PTT · P2 relay thread + tick publish · P3 client capture/playback · P4 deferred integration seam. |
| M6 Phase 1 — pure units | claude | **done ✅** | `voice_packet` (wire codec) · `voice_routing` (recipient select, deterministic) · `voice_routing_table` (double-buffer + bind queue) · `voice_jitter` · `voice_ptt`. 30 voice tests, two-stage reviewed (spec ✅ / quality ✅ + minor polish). All-new files, zero conflict. |
| M6 pure decision helpers | claude | **done ✅** | `voice_relay.process_frame` (anti-spoof re-stamp, no decode) · `server_voice.build_route_table` · `voice_playback.route`. 38 voice tests total, two-stage reviewed. Shells deferred (see below). |
| M6 P0 — Opus GDExtension | claude | **done ✅** | `native/voice_opus/` (Rust gdext **0.5.3** `api-4-6` + opus 0.3, links system libopus 1.6.1). `OpusVoiceEncoder`/`OpusVoiceDecoder` (mono f32, 48k/20ms/24kbps). **Extension loads in Godot 4.6.3 headless** (smoke hits the real round-trip, not skip); cargo round-trip test green. Two-stage reviewed — leaf rule ✅, decoder proven safe against malformed network frames (libopus errors → empty out). **ADR-0006 → Accepted.** Source committed (binary gitignored; build is local/CI). |
| M6 P2/P3 shells + integration | — | **blocked (coord + client)** | relay thread/socket shell + `server_main.gd` publish/token wiring (**shared file — coordinate with m11-destruction / M7.5-bot-AI**) + client capture/playback/connection shells (need P0 decoder + mic). |
| M6 P4 — client_main wiring + gate | — | blocked | M7 client integration (bus/keybinds/Docker port) + human-validated playtest gate. |

## Closed-milestone detail (collapsed — see the milestone docs)

Full per-task breakdowns for these **closed** milestones lived here; they duplicated the
milestone docs and the index table above, so they were collapsed during the 2026-07-11 doc
compaction. Gate evidence + task history remain in each milestone doc (and archived plans):

- **M5.5 — Combat Depth II** (P1 ballistics/fire-mode · P2 armor/suppression · P3 melee/throwables) — CLOSED ✅ 2026-06-24 → [`milestones/M5.5-combat-depth-2.md`](milestones/M5.5-combat-depth-2.md), spec [`combat-depth-2.md`](specs/combat-depth-2.md).
- **M7-P2 Audio** — engine + client wiring, owner-validated ✅ 2026-06-17 → [`milestones/M7-art-ux.md`](milestones/M7-art-ux.md), spec [`audio.md`](specs/audio.md).
- **M4.5 — Combat Depth & class identity** (P1 survivability · P2 gadgets/penetration · P3 movement) — CLOSED ✅ 2026-06-16 → [`milestones/M4.5-combat-depth.md`](milestones/M4.5-combat-depth.md), spec [`combat-depth.md`](specs/combat-depth.md).
- **M5 — Land Vehicles + substrate** — CLOSED ✅ 2026-06-16 (further vehicle work deferred) → [`milestones/M5-vehicles.md`](milestones/M5-vehicles.md), spec [`vehicles.md`](specs/vehicles.md).

## M7 — Rendered Client (Art + UX) — in-progress

Specs: [`client-prediction.md`](specs/client-prediction.md), [`hud-ui.md`](specs/hud-ui.md), [ADR-0005 renderer](adr/0005-client-renderer.md). Milestone: [`M7-art-ux.md`](milestones/M7-art-ux.md). Branch `m7-rendered-client`. Re-scoped to **P1 (playable client + HUD) → P2 (art kit + LOD)**; Steam/VAC + L3 deferred to a later online/anti-cheat track.

| Task | Owner | Status | Notes |
|---|---|---|---|
| M7 brainstorm + re-scope | claude | done | 2026-06-16; Steam + L3 deferred; phased P1→P2 (owner-approved) |
| M7-P1 specs (client-prediction, hud-ui) + ADR-0005 | claude | done | committed on `m7-rendered-client` |
| M7-P1 C1 plan + execute (core infantry loop) | claude | done | `docs/plans/2026-06-16-m7-p1-c1-infantry-client.md`; impl complete + headless-validated (milestone doc) |
| M7-P1 C2 execute (vehicles, predicted + rendered) | claude | done | landed on `m7-rendered-client` (input redundancy + FIFO jitter-buffer); see git `m7-c2` |
| M7-P1 C3 plan (combat-depth UI) | claude | done | `docs/plans/2026-06-17-m7-p1-c3-combat-depth-ui.md` — squad/scoreboard, DBNO/revive, gadget/grenade, build/destroy feedback, deploy-on-squadmate, death-recap (`ROSTER`/`SET_SQUAD`/`DEATH_INFO` + extended `SELF_STATE`/`DeploySpawn`) |
| M7-P1 C3 execute (combat-depth UI) | claude | **done ✅** | **Owner-validated 2026-06-17 (tip `dc3479c`).** 22-task plan + a long owner-playtest fix loop. Full suite green (400 run / 0 failed); ≤48 smoke PASS (winner=0, peak 15.76ms<33.3); Tasks 13 & 15 two-stage read-only reviewed (SPEC-COMPLIANT + APPROVE). Owner ran the full loop on desktop→game2: squad/scoreboard, DBNO/revive (teammate-only, "being revived" cue), death-recap (correct killer/weapon/dist/HP + damage), respawn cooldown, deploy-on-teammate, prone toggle, throwable selector, build/destroy, squad menu (U), in-bounds. Playtest surfaced + fixed many pre-existing sim bugs (pawn/vehicle map-bound clamps, seat-vacate-on-death, downed-combat rules, deploy id/slot ref keying, RPG-loadout trap → humans never roll Engineer, recap attribution to the downer). Deferred (P2/separate vehicle pass): grenade explosion VFX, corpse-on-death, vehicle riding/exit/enter. |
| M7-P1 gate (full Conquest match, placeholder art, complete HUD) | claude | **done ✅** | **PASS 2026-07-05** — owner end-to-end human playtest (laptop 1080p → game2 server + 24 bots, `conquest_town`): full match start→win, victory/defeat end screen, all close-out items confirmed. Server exits on match end (`[match] OVER winner=0 t0=48 t1=0 elapsed=169s`). Evidence: `docs/sessions/2026-07-05-m7-playtest-round6.md` + round-6/7 fix commits. |

Add new tasks here as they're discovered; promote per-milestone detail into the relevant `milestones/MX-*.md` file.
