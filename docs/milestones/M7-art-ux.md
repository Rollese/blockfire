# M7 — Rendered Client, Art Pass + UX Polish

**Status:** **in-progress** (brainstormed + speced 2026-06-16; on branch `m7-rendered-client`) · **Blocked by:** M5 land gate (done ✅) · *(pulled before M6 on 2026-06-16: this is the first human-playable rendered client; M6 voice + M10 air both need it)*

**Objective:** Turn the headless/bot-only game into the **first human-playable rendered client** — a real first-person client that predicts/renders from the shared deterministic sim — then re-skin it with the low-poly blocky kit and finish player-facing UX.

## Re-scope (2026-06-16, owner-directed)

After brainstorming, M7 was re-scoped to two phases. **Steam auth + VAC and anti-cheat L3 (line-of-sight replication culling) were both deferred** out of M7 to a later **online / anti-cheat track**: the project may stay a LAN game for family/friends (or a base for something else), so Steam's $100 + integration + ops cost isn't justified until there's a published-quality game, and L3 anti-wallhack/ESP only matters for *untrusted public* play (LAN friends don't need it, and it adds occlusion cost to a tick budget already near the edge). Both still live in [anti-cheat-matchmaking](../specs/anti-cheat-matchmaking.md) (Layers 3 & 5) for when that track is scheduled.

## Phases

### P1 — Playable client + HUD (the "it's a game now" phase)
Render the existing headless stub into a real first-person client: real input → camera → fill in prediction/reconciliation for movement/look, add client-side **ammo/fire/reload prediction** (the tracked M2/M7 gap), interpolate remote pawns + vehicles, render the world from `MapDef`/structures with **placeholder primitives** (capsules/boxes), and build the BattleBit-style HUD. Vehicles ride along (the substrate is already prediction-ready).

> **⚡ Inbound from M5.5 (owner-directed 2026-06-17):** bullets become **stepped projectiles** (drop + travel), so client fire prediction must be **projectile-aware** from Checkpoint 2 onward — the client spawns a **cosmetic local tracer** at fire (shared `shared/sim` integrator) but stays **non-authoritative** for hits (server-confirmed `KILL`/hitmarker, unchanged). Building this on hit-scan now would force a prediction refactor later. See [combat-depth-2](../specs/combat-depth-2.md) §1 / "Cross-milestone dependency".

**HUD (owner's rules):** **no health bar, no minimap;** show **ammo, compass (with objective markers), squad members, TAB scoreboard**; damage feedback via **vignette + directional arc** only. Plus crosshair, killfeed, tickets/capture status, hitmarker (server-confirmed), interaction prompts, DBNO UI, and the **death-recap card** (killer name / weapon / distance / killer HP / damage-taken breakdown — no replay killcam, no position reveal; from the 2026-06-17 gap review, needs a small `DEATH_INFO` server→victim message). See [combat-depth-2](../specs/combat-depth-2.md) §4.

**Menus:** deploy (full spawn-select, drives a new `DEPLOY_REQUEST`), minimal squad join/switch, essentials settings (sensitivity/FOV/volume/invert + renderer fallback; full keybind-rebinding deferred to P2).

**New netcode (client/server edge only):** `DEPLOY_REQUEST` (client→server, server holds humans un-deployed until they ask), `DAMAGE_EVENT` (server→victim, presentation-only), `SET_SQUAD` (minimal), `EntityState.ammo` (self-only, for ammo reconciliation). No gameplay rule logic enters `client/` (AGENTS.md §7).

**Build order (each a reviewable playtest on the owner's desktop):**
1. Core infantry loop — deploy → move (stances/lean/sprint/jump) → ADS/shoot/reload → kill/die/respawn → capture, core HUD.
2. Vehicles — enter/drive/gun/exit, predicted + rendered.
3. Combat-depth UI — squad list + scoreboard, DBNO/revive prompts, gadget/grenade use, build/destroy feedback, deploy-on-squadmate.

**P1 gate:** end-to-end **human playtest of a full Conquest match** vs bots on **placeholder art**, complete HUD, reaching a winner — judged playable + BattleBit-feeling by the owner. Recorded as evidence (owner sign-off + server log).

#### Checkpoint 1 — Core infantry loop — status (2026-06-17)

**Implementation complete; headless-validated; awaiting owner playtest.** Plan:
[`docs/plans/2026-06-16-m7-p1-c1-infantry-client.md`](../plans/2026-06-16-m7-p1-c1-infantry-client.md)
(Tasks 1–26). Runbook: [`docs/runbooks/running-client.md`](../runbooks/running-client.md).

Landed on `m7-rendered-client`:
- **Shared/server edge** — `DeploySpawn` (pure spawn-ref enumerate/validate/resolve); new wire
  messages `DEPLOY_REQUEST` / `DAMAGE_EVENT` / `SELF_STATE` + `HELLO.auto_deploy`; server honors
  `auto_deploy` (human held un-deployed → deploy screen), handles `DEPLOY_REQUEST`, emits
  `DAMAGE_EVENT` (pure `DamageDir`), sends `SELF_STATE` for ammo reconcile.
- **Client** — `WeaponPredictor` (predicted mag/reload mirroring server fire-gating, reconciles to
  `SELF_STATE`); `Prediction` extended for full-command + pitch reconcile; `WorldView`
  (snapshots + interpolation, self/remote split); `world_renderer` (placeholder primitives +
  camera + viewmodel); `hud_model` (ammo, compass, tickets/capture, killfeed, damage arcs/vignette —
  no health, no minimap) + `hud_view`; `input_map`/`input_controller`/`stance_pose`/`settings`;
  `deploy_menu` + `settings_menu`; `client_main` composition root. Renderer config + input actions +
  `client.tscn` ([ADR-0005](../adr/0005-client-renderer.md)).

Headless evidence (all green): full unit suite **351 tests, 0 failed**; ≤48-bot smoke
(`ci/m5_p1_test.sh`) **PASS** (winner valid, peak tick well under budget — server edge messages
don't regress it); end-to-end headless server+client connect reaches WELCOME → deploy → snapshots
with no errors. Two-stage review on the server-integration + composition-root tasks (read-only
reviewers); review findings (deploy-menu repopulate on death; reload-remaining reconcile) fixed.

**Remaining for C1 done:** owner playtests the full loop desktop→game2 and signs off as playable;
record sign-off + session server log here. Feel issues (look/move sign, sensitivity, recoil, HUD
layout) are follow-ups, not blockers.

#### Checkpoint 3 — Combat-depth UI — DONE ✅ (owner-validated 2026-06-17)

Plan: [`docs/plans/2026-06-17-m7-p1-c3-combat-depth-ui.md`](../plans/2026-06-17-m7-p1-c3-combat-depth-ui.md).
Landed on `m7-rendered-client` (tip `dc3479c`) — the 22-task plan plus a long owner-playtest fix loop.

**What landed (client/server-edge + presentation, no gameplay rule logic in `client/`):**
- Wire: `ROSTER` (names + per-client K/D/score), `SET_SQUAD`, `DEATH_INFO` (recap), `SELF_STATE`
  extended with `throwables` + a `being_revived` bit; `DEPLOY_REQUEST` widened to u16.
- `DeploySpawn` squadmate/vehicle refs **keyed by stable entity id/slot** (not array position, which
  aliased across the client/server edge); pure `DeathRecap`.
- HUD: scoreboard, squad roster, interaction prompt, throwable selector, **death-recap card**
  (left side, word-wrapped), respawn-cooldown countdown, "being revived — hold on!" cue, ammo/selector
  hidden while downed. Standalone **squad-select overlay (U)**. world_renderer structures + vehicle
  placeholder boxes (smoothed). deploy menu squadmate/vehicle options.

**Playtest-surfaced sim fixes** (pre-existing bugs the visual client exposed for the first time —
exactly the §10 purpose): pawn **and** vehicle movement clamp to the map's `world_half` (were hardcoded
1000, so they left small maps); **seat vacated on death** (respawn-trap); downed players can't fire and
aren't damageable (no false hitmarkers); bleed-out/give-up **credit the downer** with the kill +
correct recap (was self → "Killed by <you>", 0 m, 0 HP); killer HP/range **snapshotted at down-time**;
damage ledger reset on revive; humans **never roll Engineer** (the RPG-primary loadout had no
click-fire gun); prone is a toggle; no self-revive (teammate-only).

**Evidence:** full unit suite **400 run / 0 failed**; ≤48-bot smoke (`ci/m5_p1_test.sh`) **PASS**
(`winner=0 enters=4 veh_dead=1 rkt_veh=1`, peak **15.76 ms** < 33.3); Tasks 13 & 15 (authoritative
damage/deploy) two-stage read-only reviewed → SPEC-COMPLIANT + APPROVE. Owner ran the full loop
desktop→game2 and signed off.

**Deferred (P2 / separate vehicle-netcode pass — not C3 blockers):** ~~grenade explosion VFX~~ *(done 2026-06-24)*, ~~corpse
remains on death~~ *(done 2026-06-24)*, and vehicle riding-jitter / exit / friendly-only enter (needs `in_vehicle` + vehicle
`team` on the wire + seated-prediction suppression). **The vehicle items here are now folded into the project-wide VEHICLES DEFERRAL** (owner-directed 2026-07-05 — infantry/maps/destruction first; see the banner atop `docs/TASKS.md` and AGENTS.md §12): do not pick them up until the core is signed off. This closes the **M7-P1 build order (C1→C2→C3)**;
next is the P1 gate (full-match human playtest with complete HUD).

#### P2 increment — Grenade explosion VFX — visual-validated ✅ 2026-06-24

Thrown grenades had no detonation visual (only the RPG rocket had a client-side cosmetic arc). The reserved `DETONATION` wire message (slot 13, "M7 frag VFX") is now used: the server `_detonate` funnel broadcasts `DETONATION(pos, kind)` to all human clients (frag/impact → `DET_EXPLOSION`, flashbang → `DET_FLASH`; bots skipped), and the client spawns `WorldRenderer.spawn_explosion` — an emissive expanding fireball core + smoke puff + scattered debris (frag), or a bright white pop (flash). Server-event (not client-predicted) so it fires for **other** players' grenades too. `--boom-test` QA flag pumps explosions in front of the camera. Suite **731/0** (protocol round-trip + renderer pool/age tests); visual-validated on .128 (iGPU). Presentation-only — the server still owns the blast.

#### P2 increment — Corpse-on-death — visual-validated ✅ 2026-06-24

Dead pawns used to just vanish. The server keeps a dead pawn in snapshots (`alive=false`) for ~5s before respawn, so `world_renderer._sync_entity_pool` reliably sees the alive→false transition: when it releases an entity because it **died in view** (vs. left interest), it drops a corpse at the last pose (face-down, armor-tiered) before releasing — fires exactly once (the id leaves `_active` and dead ids are skipped on acquire). Corpses linger `CORPSE_TTL` (14s) then sink into the ground; pool capped at `CORPSE_MAX=40` (oldest recycled) to bound cost. Client-only, no wire change. Corpses always use the **procedural CharacterKit** (a static GLB has no AnimationPlayer driving a clip → renders collapsed) and skip LOD (the proxy/range cull hid them). `--corpse-test` QA flag lays a few in front of the camera. Suite **735/0** (spawn/age/cap/finite tests); visual-validated on .128.

### P2 — Art kit + LOD
Swap placeholder primitives for the low-poly blocky kit (characters, weapons, vehicles, environment) behind the same node interfaces, LOD pipeline, and audio/visual feedback polish (richer hit markers, animated damage indicators, SFX). Pure presentation on top of a proven-playable P1.

Also lands the **M5.5 presentation/feel deferrals** (the VFX/audio pieces of Combat Depth II — [combat-depth-2](../specs/combat-depth-2.md)): projectile **tracers** + muzzle flash *(done)*, **suppression** screen blur/desaturate/vignette + muffle *(screen FX done 2026-06-24; muffle done with audio)*, **flashbang** white-out + deafen *(white-out done 2026-06-24)*, melee/sledge/weapon-swap animations *(done 2026-06-24)*, fire-mode HUD indicator *(done 2026-06-24)*, armor visual diffs *(done 2026-06-24)*.

#### P2 increment — Viewmodel swing/swap animations + melee & quick-swap inputs — visual-validated ✅ 2026-06-24

The first-person viewmodel was static (no recoil/swing/swap), and `MELEE`/`SWAP_WEAPON` existed on the wire but the client sent neither — so humans couldn't melee or use their secondary. Now: `ViewmodelAnim` (pure, tested) maps phase→pos/rot offset for **SWING** (a forward-down melee jab) and **SWAP** (the gun rises into view from lowered); `world_renderer._pose_viewmodel` applies the active anim each frame on top of `VM_OFFSET`, and `set_viewmodel_weapon` auto-plays the swap anim on any weapon change (RPG/class/quick-swap). New inputs: **melee (C)** sends `MELEE` + plays the swing; **quick-swap (mouse wheel)** toggles the slot + sends `SWAP_WEAPON` (swap anim plays when `SELF_STATE` reports the new weapon). `--swing-test` QA flag freezes the viewmodel mid-slash. Suite **738/0** (3 ViewmodelAnim curve tests); visual-validated on .128 (rest vs mid-swing A/B). Client-only presentation; server still owns the hit. *Sledge note:* the Engineer sledgehammer uses the same `MELEE` path, so it shares the swing anim (a dedicated heavier sledge swing is a feel follow-up). *(Humans never roll Engineer — server `_handle_hello` — so the local viewmodel never wields a sledge anyway.)*

#### P2 increment — Viewmodel recoil kick on fire — visual-validated ✅ 2026-06-24

The viewmodel had SWING + SWAP but **no fire animation** — the gun was dead-static while shooting (the obvious missing FPS juice, universal to every player vs. the Engineer-only sledge). Adds a **RECOIL** kind to `ViewmodelAnim`: a sharp up/back impulse (muzzle rises via −pitch, gun pushed back toward the eye via +Z, a touch of roll) that snaps in at t=0 (full envelope, no ramp-up) and decays over `RECOIL_DUR` (0.11 s) via `pow(1−t, 1.6)`. On full-auto each shot restarts it from t=0, so it reads as a sustained jolt. `world_renderer.play_viewmodel_recoil(now)` reuses the existing single anim slot + `_pose_viewmodel`; `client_main` calls it at the **same fire hook that spawns the local tracer** (`_wpred.step()` returns true → every predicted shot). `--recoil-test` QA flag freezes the viewmodel near the kick peak (mirrors `--swing-test`). Suite **743/0** (1 new RECOIL curve test); visual-validated on .128 (iGPU, Wayland): rest vs `--recoil-test` A/B clearly shows the muzzle-up/back kick. Client-only presentation (AGENTS.md §7) — server still owns the hit. Per-weapon recoil magnitude + multi-shot accumulation are feel follow-ups.

#### Increment — Lighting environment (sky/sun/fog) — visual-validated ✅ 2026-06-24

The client loaded `client.tscn` with a `WorldEnvironment` whose `ProceduralSkyMaterial` had **no properties set**, so it rendered Godot's washed-out grey **default** sky with a flat, untuned sun and zero atmosphere — the world read as a flat grey **void** even though the urban map (buildings, roads, capture/base markers) was rendering correctly. This is render-the-world P1 polish, not the P2 art kit (which is meshes/textures/LOD). Fix is **data-only** in `client/client.tscn` (+23 lines, no new nodes, no `shared/`/server/wire change, no gameplay/authority touched — AGENTS.md §7): tune `ProceduralSkyMaterial` (deep-azure top → hazy horizon, tighter sun disk), `DirectionalLight3D` (warm sunlight, energy 1.15, shadows to 160 m + bias 0.04), and `Environment` (Filmic tonemap, sky-driven ambient ~0.9, light distance fog density 0.0017 + aerial perspective so the flat terrain and distant buildings gain depth/scale). The camera shares the `World3D`, so the env applies regardless of which `Camera3D` renders. Suite **743/0** (no test touches the .tscn; confirms no load regression); **visual-validated on .128** (iGPU, RADV Renoir, Wayland) over the Town map — controlled HQ A/B (`--deploy=0`, fixed spawn) shows grey void → blue sky + warm sun + atmospheric haze, and in-town squadmate-deploy shots show buildings with lit/shadowed faces reading as a real scene. Sky-color/fog-density feel-tuning is an owner playtest follow-up.

#### Increment — F3 debug-overlay toggle — ✅ 2026-06-24

The green fps/draws/vram perf readout was always on screen with no way to hide it short of F8 free-fly photo mode (which also takes over the camera). Added an **F3 toggle** (`HudView.set_perf_visible` + `client_main._poll_debug_overlay_key`, edge-detected in `_process` like the F12/F9 screenshot + F8 photo polls) — **default ON** so the dev workflow is unchanged; flip it off for a clean HUD during play / screenshots. `_process` early-returns (skips the `Performance` reads) while hidden. Client-only (AGENTS.md §7). Tests `hud_view_perf_toggle_test.gd` (default-on / toggle / null-safe-before-build); suite **746/0**.

#### Increment — Capture/base zone rings (kill the screen-filling disc) — visual-validated ✅ 2026-06-24

Capture points and bases rendered as a big **solid-color filled cylinder disc** (`_make_cylinder_marker`, base = `b_radius*0.4` wide), so deploying at HQ filled the lower half of the screen with a flat blue blob and markers read as blobs, not zones. Replaced with a flat ground **ring** (`_make_ring_marker`, `TorusMesh` lies in XZ) at the **true** capture/base radius — BattleBit zone read — emissive + semi-transparent + unshaded so it glows at any range without dominating the view. Tall colored beacons (unchanged) stay the across-map nav landmark; the HUD capture bar stays the authoritative "in the zone" feedback. Client-only cosmetic (`world_renderer`), no wire/gameplay change. Tests `world_renderer_ring_marker_test.gd`; suite **749/0**; visual-validated on .128 (HQ A/B — blue blob gone; in-town — ring lies flat, beacon intact).

#### Increment — Procedural two-tone ground — visual-validated ✅ 2026-06-24

The ground was one flat muted-green `PlaneMesh` (an infinite uniform floor, no depth cue). Gave it an in-engine procedural noise albedo (`FastNoiseLite` simplex → `NoiseTexture2D`, seamless, two-tone green `color_ramp`, ~70 m tiling via `uv1_scale`) so open areas show low-frequency grassy patches — a subtle depth cue, not a loud pattern; roads/buildings sit on top so only open terrain shows it. No asset file, client-only cosmetic (`world_renderer`), no wire/gameplay change. Suite **749/0**; visual-validated on .128 (open-field shots show mottled terrain).

#### Increment — Dynamic spread crosshair — visual-validated ✅ 2026-06-24

The crosshair was a static `"+"` label. Replaced with a **4-tick reticle + centre dot** whose gap blooms with honest client-side spread — movement speed, airborne, and each shot widen it; crouch/prone tighten it; it returns to a small resting gap when still. Pure client feedback off the predicted pawn's `velocity`/`stance`/`grounded` + a per-shot `_ch_fire_bloom` that decays each frame (kicked at the same predicted-shot hook that fires the tracer/recoil). `HudView.update_crosshair(spread, hidden)` repositions the ticks each frame (and pulls the reticle while dead/downed/deploying). `--crosshair-test` QA flag forces a bloomed reticle. No wire/gameplay change (AGENTS.md §7). Tests `hud_view_crosshair_test.gd`; suite **753/0**; visual-validated on .128 (resting-tight vs `--crosshair-test`-wide A/B).

#### Increment — Viewmodel locomotion (walk bob + sprint-lower) — visual-validated ✅ 2026-06-24

The first-person weapon was dead-static while moving. Added a continuous locomotion offset composed on top of the base placement + any one-shot swing/swap/recoil: a **walk bob** (subtle step-cadence sway scaled by horizontal speed — zero at a standstill, <5 cm at full speed, Y-down-weighted so each step dips) and a **sprint-lower** (when sprinting = stand + grounded + speed>6.5, the gun dips and angles out of the aim, eased in/out). Pure math in `ViewmodelAnim` (`walk_bob` + `SPRINT_LOWER_*` consts); the renderer advances the bob phase by distance travelled and eases the sprint amount from the predicted pawn's `velocity`/`stance`/`grounded`. Client-only (AGENTS.md §7). `--sprint-test` QA flag freezes the lowered pose. Tests `art_viewmodel_locomotion_test.gd`; suite **757/0**; visual-validated on .128 (sprint-lowered vs in-aim A/B). Per-weapon bob tuning is a feel follow-up. **+ Sprint FOV kick:** the same eased `_vm_sprint_t` widens the camera FOV by up to +8° while sprinting (`_apply_camera`) for a sense of speed — cohesive with the sprint-lower, settles back when not sprinting; visible under `--sprint-test`.

#### Increment — Reload viewmodel animation — visual-validated ✅ 2026-06-24

The viewmodel had swing/swap/recoil but **no reload gesture** — reloading (one of the most frequent actions) left the gun dead-static. Added a `RELOAD` kind to `ViewmodelAnim`: the gun cants inward + dips to work the magazine (a trapezoid envelope so it reads over the whole, *variable* reload duration) with a couple of downward taps, then returns to rest. Triggered from the existing reload-start transition in `client_main` (alongside the reload sfx), stretched over the real per-weapon `reload_secs` via `play_viewmodel_reload(now, dur)` (reuses the one-shot anim slot + `_pose_viewmodel`). Client-only (AGENTS.md §7). `--reload-test` QA flag freezes it mid-reload. Tests `art_viewmodel_reload_test.gd`; suite **760/0**; visual-validated on .128 (canted-to-magwell vs in-aim rest).

#### Increment — Bullet impact effects (server `IMPACT_FX`) — validated ✅ 2026-06-25

Bullets vanished where they hit the world — no feedback that a round chipped a wall or kicked dirt. Added a cosmetic impact effect at the two clean projectile-termination points, mirroring the `SHOT_FX` cosmetic-message pattern (the owner-approved additive-client boundary — this is the first M7 increment to cross into `shared/`+`server/`, all the rest are client-only). **Wire:** new `IMPACT_FX` message (Msg 34) + `IMPACT_WALL`/`IMPACT_DIRT` surface kinds + codec (pos ×10, kind byte). **Server:** `_broadcast_impact_fx` (humans-only, unreliable, **per-tick capped at 24** to bound fan-out at bot scale) emitted from `_step_projectiles` when a bullet stops on a non-penetrable structure (wall) or hits the ground (`y<=0`, segment lerped to the surface) — presentation-only, no gameplay/authority change. **Client:** `IMPACT_FX` → `WorldRenderer.spawn_impact` = a small kind-coloured dust puff + chips (reuses the puff/debris pools; `_spawn_puff` gained an optional colour). `--impact-test` QA flag. Tests `protocol_test` (round-trip) + `world_renderer_impact_test` (puff+chips, non-finite guard, age-out). Suite **764/0**. **Server no-regression:** `ci/m5.5_p1_test.sh` PASS — `proj=117 projhit=20` (impacts emitted live in-match), peak tick **15.68ms** < 33.3. Client effect visual-validated on .128. **+ Flesh + pass-through (2026-06-25):** added `IMPACT_FLESH` (kind 2 — a smaller red blood mist, 2 droplets, no hard chips) emitted on a pawn hit at `old_pos + seg_dir*best_t`, and a pass-through wall-dust puff when a bullet punches through a *penetrable* structure (IMPACT_WALL at the carve point). All four bullet-termination cases now show feedback (wall-stop, penetrate, ground, flesh). Suite 765/0; `--impact-test` cycles all three kinds (visual-validated on .128 — grey/brown/red distinct). **+ Downed-pawn flesh (2026-06-25):** a bullet through a *downed* enemy (immune to finishing — BattleBit) now emits a cosmetic blood impact at the hit point while still dealing no damage and never blocking, so you get visual feedback that you're firing on someone already down. Test `projectile_gate_test::test_downed_enemy_takes_no_projectile_damage_but_emits_blood` (protects the no-finishing invariant + exercises the emit). Suite 766/0. **+ Impact audio (2026-06-27):** the `impact` (concrete thud) sound was a dead catalog entry — bullet hits were silent. `WorldRenderer.spawn_impact` now emits an `impact(pos,kind)` signal (mirroring the footstep-signal pattern) that `client_main._on_impact` routes through `client/impact_audio.gd` (`ImpactAudio.sound_for`, pure/tested) to a spatial thud — **wall/dirt thud, flesh stays silent** (a concrete tink on a body reads wrong; it already gets a blood puff). Routing through `spawn_impact` means both real `IMPACT_FX` and the `--impact-test` demo trigger it (one code path). View-only, no wire/server change. 4 mapping tests; suite **787/0**. Validated: live `--impact-test` run logged 131 events with correct wall/dirt→`impact`, flesh→silent mapping, and the .128 screenshot shows the impact puffs in the GUI client.

#### Increment — Vehicle destruction (burnt hulk + fireball) — visual-validated ✅ 2026-06-27

A destroyed vehicle was **a dead client event**: the server sends `VEHICLE_DESTROYED` (Msg 19) reliably to every client on `hp→0` (`_destroy_vehicle`), but `client_main` had **no handler** — and the renderer ignored the replicated `VehicleState.hp`. So a blown-up transport kept rendering as a fully-intact, enterable-looking box for the whole ~15 s respawn window (450 ticks), with no explosion and no wreck — then silently snapped back to pristine on respawn. Added both halves:

- **Persistent wreck look (renderer, driven by replicated `hp<=0`):** `_sync_vehicle_pool` now calls `_set_vehicle_wrecked(node, vs.hp <= 0)` each frame — a destroyed vehicle chars every body mesh to a blackened `_wreck_material` and leans the hulk (`WRECK_TILT` ~8°), plus a slow smouldering smoke column (`_ensure_wreck_smoke`, throttled per visible wreck via node meta). It's **self-correcting**: a respawn restores `hp>0` → the original materials + upright pose come back (idempotent, guarded by a node meta flag; original materials cached per-child in meta; nodes un-wrecked before pooling so recycled/respawned vehicles start clean). Robust for vehicles that come into view already destroyed or respawn out of sight — **no event needed for the persistent state**, so it never desyncs.
- **One-shot destruction VFX + audio (event-driven):** `client_main` handles `VEHICLE_DESTROYED` → `WorldRenderer.destroy_vehicle` = a big fireball + smoke + scattered debris at the vehicle's last known position (reuses the explosion pools), plus a spatial `explosion` audio cue — exactly the `DETONATION → spawn_explosion + play_at` pattern. **Deliberately does NOT set the wreck state from the event** (avoids a flicker: the reliable event can beat the `hp=0` snapshot by a stagger/interest window, so an immediate event-wreck would be un-wrecked by the next frame's still-stale `hp>0` snapshot; the `hp=0` snapshot lands within a frame or two, under the fireball's lifetime which covers the gap).
- Client-only — no `shared/`/server/wire change (the message + server emit already existed). `--vehicle-test` QA flag blows up a transport in front of the camera and leaves the smoking hulk for a screenshot. Suite **823/0** (no new tests — view-only, like the other VFX increments; reuses the proven explosion/puff/debris pools). **Visual-validated on .128** (iGPU/Wayland): the destruction frame shows the orange fireball + debris; a few seconds later the burnt, tilted black hulk smoulders in place (vs. the intact neutral transport before). Burn-shade/wreck-mesh feel-tuning is owner-gated (AGENTS.md §10).

**+ Damaged-vehicle smoke (2026-06-27):** completed the vehicle-damage-feedback picture — a vehicle now smokes *before* it dies, BattleBit-style. The renderer lazy-loads `VehicleCatalog` (`data/vehicles.json`) to know each type's `max_hp`, computes `ratio = vs.hp / max_hp` per frame, and `_ensure_vehicle_smoke` emits a throttled puff: healthy = none, below `DAMAGE_SMOKE_RATIO` (0.5) = a light grey wisp that thickens and quickens toward death, `hp<=0` = the heavy near-black wreck column. (The wreck-smoke path was generalised into this one function.) Driven purely by the already-replicated `hp`/`type` — no wire/server change. `--vehicle-test` now stages **both** a destroyed hulk and a heavily-damaged (ratio 0.2) transport side-by-side; **visual-validated on .128**: the burnt hulk's dark column vs. the intact-tinted damaged transport's lighter grey wisp read as clearly distinct. Suite **823/0** (view-only). Merged to master local (tip below). Smoke colour/threshold feel-tuning is owner-gated (AGENTS.md §10).

#### Increment — Aim-down-sights (ADS) zoom + DMR scope — visual-validated ✅ 2026-06-27

The `aim` input action (right-mouse) was bound but **read nowhere** — there was no ADS at all: the camera FOV never tightened, the viewmodel never moved to the sight line, and the DMR loaded a `sniper_rifle.glb` (with a modelled scope) you could never look through. Added a **client-only visual ADS** (BattleBit-style). Held `aim` while deployed / not sprinting / not in a vehicle eases an `_ads_t` (0→1) that drives, all blended:
- **FOV zoom** to a per-weapon ADS FOV (`_ads_fov`, fraction of the player's hip FOV setting): DMR ×0.38 (scoped), RPG ×0.88 (barely), AR/SMG/pistol ×0.72 (iron). client_main owns the FOV and passes it to `WorldRenderer.update`.
- **Zoom sensitivity**: `InputController.apply_look` gained a `look_scale` (= ADS FOV ÷ hip FOV) so the on-screen angular look speed stays ~constant as you zoom — no twitchy aim. (Unit-tested: `input_controller_test::test_look_scale_reduces_rotation_proportionally`.)
- **Viewmodel sight pose**: `WorldRenderer.set_ads` lerps the gun to `VM_ADS_OFFSET` (inboard + braced low so it reads as aiming without filling the view — the procedural weapons have no aligned iron sights) and damps the locomotion bob (steadier hold).
- **Hip crosshair hides** once aiming (the sights/scope take over).
- **DMR scope overlay**: a pure canvas shader in `HudView` (`set_scope`, mirroring the suppression overlay) — an aspect-correct black circular mask + edge ring + crosshair hairlines, fading in with `_ads_t`, only for a scoped weapon (`_is_scoped` → DMR). While scoped the viewmodel is **hidden** (`set_viewmodel_scope_hidden`) so you look *through* the scope, not at the centred gun (composes with the photo-mode hide).

Client-only — no `shared/`/server/wire change (server-authoritative spread-reduction-on-ADS would need an input bit, but the `buttons` mask is a full u8; that's a netcode follow-up, noted). The DMR's base spread is already tiny, so the zoom is the real benefit (not misleading). `--ads-test` forces iron ADS, `--scope-test` forces ADS + the scope overlay (the client can't pick the DMR class via CLI). Suite **824/0**; **visual-validated on .128** (iGPU/Wayland): hip vs iron-ADS (zoom + braced gun + no crosshair) vs DMR scope (circular mask + reticle + hidden gun) all read clearly distinct. ADS pose/FOV feel-tuning + server-side spread-on-ADS are owner/netcode follow-ups (AGENTS.md §10).

#### Increment — Server-authoritative ADS spread reduction (netcode) — validated ✅ 2026-06-27

The M7 ADS increment was client-visual-only (FOV/scope/pose) — aiming gave no actual accuracy benefit because the spread model never knew you were aiming. Closed that with the wire + sim change it was waiting on:
- **Wire:** `InputCommand.buttons` widened **u8 → u16** (it was a full 8 bits) and a `BTN_AIM` (bit 9) added. `+1 byte per input frame` — negligible (input is one client's frames, not the snapshot hot path). All three roles share `InputCommand`, so client/bot/server stay in lockstep.
- **Input:** `input_map.gd` maps the (previously dead) `aim` action → `BTN_AIM`; `input_controller.gather` sets it. The in-vehicle / photo-mode button masks already drop it (no ADS there).
- **Sim:** `Combat.reconstruct_ray` gained an `aiming` arg → multiplies the whole accumulated cone (base + bloom + move + suppression) by `ADS_SPREAD_MULT` (0.35), applied **before** the prone-bipod zero-override so a deployed bipod still wins.
- **Server authority:** `_fire_shot` honours `BTN_AIM` only when **not sprinting** (you can't physically ADS mid-sprint), so a client can't claim ADS accuracy while sprint-running. The server reconstructs the authoritative ray as always — clients send intent, not the cone.
- Crosses `shared/`+`server/` (a real gameplay/netcode change, not a cosmetic broadcast). **Proven deterministically (AGENTS §10):** `combat_test` — ADS deviation < hip and scales exactly by `ADS_SPREAD_MULT` (isolated at shot_index 0 to exclude the recoil climb, which ADS doesn't reduce); bipod-zero still wins over ADS; `input_command_test` — `BTN_AIM` survives the u16 round-trip; `input_map_test` — `aim`→`BTN_AIM`. Suite **839/0**. **No-regression smoke** (24 bots / dev_arena / ~20 s): clean — bots connect/move/fire, `shots`/`proj`/`projhit`/kills/heals register, tick ~9–10 ms, zero input-decode errors (the u16 widening is transparent to live bot/server input). Hip-fire is unchanged; the client already hides the crosshair on ADS, so the tightened cone reads honestly.

#### Increment — Audio gaps: flashbang cue + vehicle engine loop — validated ✅ 2026-06-27

Two dead `data/sounds.json` catalog entries, now wired:
- **Flashbang cue:** the `DETONATION` handler always played the frag `explosion` boom regardless of kind — so flashbangs (the `flashbang` entry) were never heard. Now `DET_FLASH` → `flashbang`, else `explosion` (one conditional; the white-out VFX already keyed off the kind).
- **Vehicle engine loop:** `engine` (an `engine_loop` stream) was defined but **never played**. Added a single dedicated looping engine voice (`AudioDirector._engine_player`, outside the one-shot pool) + `update_engine(pos)`/`stop_engine()`. `client/audio/engine_audio.gd` (`EngineAudio.nearest_running`, pure/unit-tested) picks the nearest **running** vehicle each frame — alive (`hp>0`) AND (driver seat occupied OR it's the vehicle you're riding) — within `ENGINE_AUDIO_RANGE` (200 m); `client_main` voices it or stops. The synth `engine` placeholder is regenerated as a **seamless loop** (`_gen_engine_loop`: a whole number of fundamental periods + a 2nd harmonic + `LOOP_FORWARD`; the one-shot `_gen_tone` decays to zero and would click each wrap). `gunfire_supp` stays dead by design — no suppressed-weapon variant exists yet (not a gap).
- Client-only; no wire/server change (engine state derives from already-replicated `VehicleState.seats`/`hp`). **Validation:** suite **835/0** (6 `EngineAudio` selector tests + a seamless-loop test); a live client on .128 runs the per-frame selector clean (driverless arena → `stop_engine` path) and `--engine-test` forces `update_engine` (stream-load + play + reposition) with no runtime errors. **Auditory quality is owner-gated** (placeholder synth tones until real CC0 assets — audio.md track); this wires the dead entries + spatialization, not final sound design.

#### Increment — Authored GLB shoot clip (holding-both-shoot) — visual-validated ✅ 2026-06-27

Upgrades the remote fire-recoil for the **GLB character path** (`use_models`) from the procedural body-pitch twitch to the Kenney rig's **authored `holding-both-shoot` clip** (one of the GLB's 27 node-transform animations; the resting hold is `holding-both`). `CharacterAnim.clip_for` gained a `firing` arg: stationary + firing → `holding-both-shoot` (looped, so sustained auto-fire keeps cycling the recoil); a *moving* shooter keeps its walk/sprint clip (the fire feedback there is the muzzle flash/casing/tracer, not a full-body pose swap). `_pose_entity` derives `firing` from the same `_fire_recoil` window the body-pitch twitch uses (armed by `SHOT_FX` `shooter_id`), so both render paths share one trigger. The procedural body-pitch recoil is now **gated to `not use_models`** so GLB doesn't double the recoil (clip + pitch). Client-only — no wire/server change (`shooter_id` already landed with the recoil-twitch increment). `--glbshoot-test` builds two GLB soldiers side-by-side driving `holding-both` vs `holding-both-shoot` (independent of the `use_models` setting). 3 new `CharacterAnim` tests (firing→shoot, moving-keeps-locomotion, idle→plain-hold); suite **828/0**; **visual-validated on .128** — frame-sampling the looping shoot clip shows the soldier's arms cycling through the recoil (arms drop/return) vs. the steady hold. A richer authored climb/jump clip is still a follow-up.

#### Increment — Remote fire-recoil body twitch (SHOT_FX shooter_id) — visual-validated ✅ 2026-06-27

A remote shooter already got a tracer, muzzle flash (`tracer_from` → `_spawn_flash`), casing, and gunfire audio — but its **body stood statue-still** behind all that. Bound a brief fire-recoil to the pawn: `SHOT_FX` gained a trailing `shooter_id` (u32, backward-compatible decode); the client routes it to `WorldRenderer.flash_fire(id)`, which arms a short (`FIRE_RECOIL_TTL` 0.13 s) lean-back twitch keyed by pawn id. `_pose_entity`'s standing branch applies a facing-relative recoil pitch (`_fire_recoil_pitch`, eased out) about the body's right axis — built with `rotated()` so it reduces exactly to the old `from_euler(0,yaw,tilt)` when idle (no change to the validated standing/lean pose). Repeated on auto-fire the body visibly flinches with each shot instead of being a turret. Server passes `shooter_id` to `encode_shot_fx` (it already had it for the shooter-exclude); `_broadcast_shot_fx` still excludes the shooter so the local player's own body isn't double-driven. Crosses `shared/`+`server/` (one additive wire field). `--firepose-test` pins an upright dummy beside one frozen at peak recoil. Suite **825/0** (shot_fx round-trip test extended for `shooter_id`); **visual-validated on .128** — the recoiling dummy leans back vs. the upright one. A richer authored GLB shoot clip (for `use_models`) is a follow-up; this body-pitch works for both procedural + GLB.

#### Increment — DBNO self-bandage feedback — visual-validated ✅ 2026-06-27

Self-bandage was a **dead feature**: the server fully implements it (`_handle_self_bandage` — a downed player spends a bandage charge to set `bleed_halted`, permanently halting the bleed-out until revive/give-up), but the client **never sent `SELF_BANDAGE`** (`client_main` literally `pass`-ed with a comment saying "downed players have no self-bandage") and the downed overlay showed only a bleed-out timer — no bandage prompt, count, or stabilized state. Wired it end-to-end:
- **Input:** while downed, **R** (`reload`, free in DBNO) sends `SELF_BANDAGE` (client-gated on having a charge + not already halted; server re-gates authoritatively).
- **State to the owner:** `SELF_STATE` now carries `bandage_count` (u8) + `bleed_halted` (bit), appended last so the codec stays backward-compatible (decoded behind the existing `get_available_bytes()` guards). Owner-only, rides `SELF_STATE` like suppression/blind. Server passes `self_pawn.bandage_count`/`bleed_halted`.
- **UI** (`HudView.set_downed` gained `bandage_count`/`bleed_halted`): a prompt line — amber "Press [R] to bandage (N)" / grey "No bandages left" — and, once bandaged, the timer turns cyan "Stabilized — N s left" with "Bleed stopped · N bandages left". `client_main` **latches** the local bleed-out estimate the instant `bleed_halted` arrives (the timer is a client-side estimate; without this it'd keep counting down past the halt).
- Crosses into `shared/`+`server/` (additive `SELF_STATE` fields — the owner-approved boundary, as the suppression/blind bytes did). `--downed-test` QA flag forces the overlay, alternating the bleeding-prompt and stabilized states. Suite **825/0** (+1 codec test: `protocol_test::test_self_state_carries_bandage_state`); **visual-validated on .128** — both states render correctly (amber "Press [R] to bandage (3)" while bleeding; cyan "Stabilized"/"Bleed stopped · 3 bandages left" after). Note: one bandage halts bleed-out for the rest of that down (matches the server rule — a deliberate divergence from BattleBit's heal-over-time; default-to-code).

#### Increment — Landing FX + airborne (jump/fall) pose — visual-validated ✅ 2026-06-27

Jumping/falling had no presentation: remote players in the air rendered bolt-upright (gliding), nothing kicked up on touchdown, and the local view didn't react to a hard landing. `grounded` is replicated only on the local predicted pawn (not in the snapshot), so remotes need it **inferred**. Added `WorldRenderer._update_airborne(id, es, dt)` — a per-remote smoothed vertical-velocity estimate from interpolated-y deltas (continuous, called every frame before the pose branches) that (a) returns an airborne flag and (b) kicks a **landing dust burst** at the feet on the fall→settle edge (gated on having fallen faster than `AIR_FALL_VY`, so it doesn't fire at a jump apex). Drives two effects:
- **Airborne pose** (`_pose_entity`): while airborne, a forward tuck (`JUMP_PITCH`) + mild vertical compress (`JUMP_TUCK`) so an off-the-ground figure reads as leaping/falling rather than gliding upright. (Same shared pose path → applies to procedural + GLB; first-person local is unaffected.)
- **Landing dust** (remote): a strong dust burst (reuses `_spawn_footstep_fx`) at the touchdown point.
- **Local viewmodel land-dip**: `client_main` watches the predicted pawn's airborne→grounded edge after a real fall (`_prev_vy < -LAND_VY`) and calls `WorldRenderer.play_land_dip(strength)` (impact-scaled) — a downward viewmodel kick that eases back in `_update_viewmodel_locomotion`.

Client-only — no wire/server change (vertical motion is inferred, not newly replicated). `--jump-test` pins an airborne-posed dummy beside an upright one; `--land-test` pumps landing dust + the dip. Suite **824/0** (view-only); **visual-validated on .128** — the airborne dummy clearly leans/tucks vs. upright, and a dust scuff pops at the landing point. Heuristic vy thresholds may misfire on steep ramps/elevators (rare, cosmetic-only); authored GLB jump/land clips are a follow-up.

#### Increment — Ladder-climbing pose — visual-validated ✅ 2026-06-27

The `climbing` flag is replicated (snapshot bit 7 → `EntityState.climbing`) and reached `StancePose`, but the renderer only used it to gate footstep dust — so a remote player on a ladder rendered **bolt-upright, sliding up** (reads as floating/broken). Added a climbing branch in `WorldRenderer._pose_entity`: when `es.climbing`, pitch the figure forward `CLIMB_PITCH` (~18°) about its feet (same sign convention as the prone lay-down) while keeping the stance height scale, so a climber leans into the rungs instead of standing. First-person is unaffected (you don't see your own body); this is purely how *other* players read on ladders. Client-only, no wire/server change (the flag was already on the wire). `--climb-test` QA flag pins an upright dummy beside a climbing-posed one. Suite **824/0** (view-only); **visual-validated on .128** — the climbing dummy clearly leans forward vs. the upright one. GLB characters get the same basis pitch via the shared pose path; an authored climb clip is a follow-up.

#### Increment — Shell-casing ejection — visual-validated ✅ 2026-06-27

Firing kicked a muzzle flash + tracer + recoil but ejected **no brass** — the most-performed action lacked that bit of kinetic juice. Added a local-player casing eject on each shot (non-RPG): `WorldRenderer.eject_casing` spawns a small brass cylinder in **world space** at the camera/eye (so it tumbles + falls independently of the view, not glued to it), popping **up-and-right** out of the port (a pure rightward eject leaves the frame instantly — biased upward so the brass is briefly visible near the gun before falling away) with per-shot variation (counter-based, no per-frame RNG), gravity + a small ground bounce + tumble, a 3 s TTL, and a `CASING_MAX` (40) live cap (drop-oldest) so sustained auto-fire stays bounded. Hooked at the existing local-fire point in `client_main` (`_wpred.step` returns true), skipped for the RPG. Client-only, no wire/server change; reuses the debris-pool aging pattern. `--casing-test` QA flag pumps casings on a cadence. Suite **824/0** (view-only); **visual-validated on .128** — a brass case tumbles up-and-right from the gun. **+ Remote-player casings (2026-06-27):** `SHOT_FX` already carries origin+dir (the `shooter_id` blocker only applied to the fire *anim*), so `eject_casing_at(origin, dir)` ejects a casing at a remote shooter's muzzle (right of the aim, derived from `dir`) on each `SHOT_FX`. `eject_casing` (local) + `eject_casing_at` (remote) share one `_spawn_casing` core. No double casing on the local player — `_broadcast_shot_fx` already excludes the shooter. `--casing-test` now also pumps a downrange remote casing; visual-validated on .128 (local brass near the gun + smaller brass downrange).

#### Increment — Footstep FX (dust + spatial audio) — visual-validated ✅ 2026-06-26

Footsteps were a **dead catalog entry**: `data/sounds.json` defined a `footstep` sound and `AudioDirector` could synth a tone for it, but **nothing in the client ever triggered one**, and moving pawns kicked up no dust. Added `client/footstep_cadence.gd` (`FootstepCadence`, pure/unit-tested): a pawn fires a footstep every **stride** of ground travel (STAND 2.2 m / CROUCH 1.4 m / PRONE silent) — distance-based so cadence is frame-timing-independent, gated on grounded + a min speed, with a teleport/respawn-jump reset; `intensity` scales with speed. **Renderer** (`world_renderer.gd`, view-only AGENTS.md §7): remotes accumulate ground travel between authoritative pos updates in `_sync_entity_pool` (per-id accumulators cleaned on release) → a faint tan dust scuff at the feet + a `footstep` signal; the local pawn uses predicted velocity/grounded/stance (time-based, audio only — feet are below the eye, emitted at the eye = centred). **`client_main`** routes the `footstep` signal to `AudioDirector.play_at` (spatial; `max_distance=25 m` + priority-0 voice-stealing bounds 128p footstep spam). No wire/gameplay change; audio plays placeholder synth tones (real asset + feel are owner-gated). `--footstep-test` QA flag pumps dust in front of the camera for a deterministic screenshot. 8 cadence tests; suite **774/0**; **visual-validated on .128** (iGPU/Wayland) — A/B dust scuffs vs clean ground.

#### Increment — Bullet crack/whiz near-miss audio — validated ✅ 2026-06-27

Another **dead catalog entry**: `data/sounds.json` defined `bullet_crack` + `bullet_whiz` and `AudioDirector` could synth tones for them (and `audio.md` lists "bullet crack/whiz from M5.5 projectiles" as intended), but **nothing ever triggered them** — a remote round screaming past your head was silent. Added `client/bullet_passby.gd` (`BulletPassby`, pure/unit-tested): given a shot's ray (origin + unit dir) and the local eye, it finds the closest approach of the ray to the eye and classifies it — within **1.6 m → `bullet_crack`** (sharp supersonic snap), within **5 m → `bullet_whiz`** (softer zip), else silent; rejects shots flying *away* from the eye (closest approach behind the muzzle) and degenerate/zero rays, and clamps to the round's max travel. **Wiring** (`client_main`, SHOT_FX handler): every remote shot already arrives as `SHOT_FX(origin,dir)` — the server **excludes the shooter and bots**, so each one is genuinely an incoming remote round — so the existing handler now also runs `BulletPassby.classify` against the predicted eye and, on a hit, plays the crack/whiz **spatially at the closest-approach point** (so it pans to the side the round actually passed). **No wire/server/sim change** — view-only (AGENTS.md §7), reusing the already-replicated SHOT_FX. 9 geometry tests; suite **783/0**. `--whiz-test` QA flag sweeps a synthetic round flying toward the player from 40 m downrange, alternating a 1 m (crack) / 2.5 m (whiz) miss, with an always-on tracer for a screenshot. **Validated on .128** (iGPU/Wayland): live `[whiz-test]` log shows correct `bullet_crack`/`bullet_whiz` alternation through the real client audio path, and the screenshot shows the near-miss tracer streaking past the player. Placeholder synth tones until a real asset (owner-gated). **+ Melee swing whoosh (2026-06-27):** the local quick-melee (C) played the viewmodel swing but no sound — the catalog's `melee` entry was never triggered; now plays `melee` (2D, like the reload whoosh) on swing so your own attack has audible feedback (hit-confirm stays the hitmarker). One-line view-only add; suite 783/0.

#### Increment — Killfeed name resolution — visual-validated ✅ 2026-06-27

The killfeed showed raw player **ids** ("11 → 7") — meaningless to a player. Now it resolves names + friend/foe colour from the already-available roster. View-only, no wire/server change. **`HudModel._killfeed_current`** builds an id→roster map from `ctx["roster"]`, derives the local team from `self_id`, and enriches each entry with `killer_name`/`victim_name` (falling back to `#id` for an unknown id) + `killer_friendly`/`victim_friendly` (team vs local). **`HudView`** renders "Killer → Victim ☆", tinted **blue** when the killer is friendly / **red** when hostile. 2 model tests (resolution + unknown-id fallback); suite **820/0**. `--killfeed-test` QA flag pumps named kills with a fake roster; **visual-validated on .128** — the feed reads "Vanguard → Reaper" / "Specter → Falcon" colour-coded, not bare ids.

#### Increment — Capture-point announcement banners — visual-validated ✅ 2026-06-27

In a Conquest game you never learned an objective flipped unless you were standing on it. Added center-top **capture banners** ("A CAPTURED" / "B LOST" / "C NEUTRALIZED"). View-only — derived from the already-broadcast `MATCH_STATE` point owners; no wire/server change. `client/capture_announce.gd` (`CaptureAnnounce.diff`, pure/unit-tested): diffs previous vs current owner arrays and classifies each change relative to the local team — `FRIENDLY` (we captured), `HOSTILE` (enemy captured / we lost), `NEUTRAL` (contested away), handling unknown team + size mismatch. **`client_main`** tracks `_prev_point_owners`, diffs each `MATCH_STATE`, and pushes `{label (point id), status}` events to the HUD. **`HudModel`** holds a time-bounded `_capture_feed` (mirrors the killfeed; `CAPTURE_FEED_TTL` 4 s). **`HudView`** renders a stack of up to 3 centred banners (newest on top), coloured green/red/yellow and fading out over the last second. 8 diff tests; suite **818/0**. `--capture-test` QA flag pumps one banner of each kind; **visual-validated on .128** — green CAPTURED / red LOST / yellow NEUTRALIZED stack reads clearly at top-centre.

#### Increment — Grenade danger indicator — visual-validated ✅ 2026-06-27

Completes the grenade loop: with grenades now visible in the air (local arc + remote `GRENADE_FX`), a HUD **danger indicator** warns when a live one lands near you (a BattleBit/CoD staple). View-only — reads the renderer's cosmetic grenade pool. **`WorldRenderer.live_grenade_positions()`** exposes the live `_thrown` positions; `client_main` feeds them (plus `self_eye`) into the HUD ctx. **`HudModel._grenade_danger`** (pure/unit-tested): the nearest grenade within `GRENADE_DANGER_RADIUS` (7 m) → `{rel_bearing, proximity}` (same bearing convention as the compass/damage arcs), or `{}` when nothing's close. **`HudView`**: a distinct **orange** directional triangle (reuses the `_ArcMarker` with a new `tint`/size/dist) pointing at the threat, **pulsing** with a sine and brightening with proximity. 4 model tests; suite **810/0**. `--grenade-danger-test` QA flag pins a live grenade ~3 m from the player; **visual-validated on .128** — the orange arrow points at the nearby grenade, distinct from the red incoming-fire arcs.

#### Increment — Downed-teammate revive marker — visual-validated ✅ 2026-06-27

A downed (DBNO) friendly showed the **same steady blue triangle** as an alive one — so in a 128-player match you couldn't tell who was down and needed picking up. `is_downed` is already replicated (the renderer poses DBNO pawns face-up), so this is purely a marker tweak. `client/revive_pulse.gd` (`RevivePulse.bob`, pure/unit-tested): a `(1-cos)/2` vertical bob (starts at rest, bounded to `AMP`) so the marker draws the eye. **Renderer**: in the friendly-marker branch, a downed teammate's marker is re-tinted to `REVIVE_MARKER_COLOR` (red-orange) and bobs via `RevivePulse.bob`; an alive teammate stays the steady blue `ArtPalette.FRIENDLY` marker. Reuses the existing pooled marker (just a per-frame colour + Y), no second pool. View-only, no wire/server change (uses already-replicated `is_downed`). 4 pulse tests; suite **806/0**. `--revive-marker-test` QA flag lays a downed friendly with the marker; **visual-validated on .128** — the red revive triangle reads clearly above the face-up body, distinct from the blue alive marker.

#### Increment — Deployed gadget rendering (C4 / mines / supply bags) — visual-validated ✅ 2026-06-27

Deployed gadgets were **invisible**: the server placed and tracked C4 (`_c4`), claymore mines (`_mines`), and supply bags (`_bags`), but never told the client — placing C4 on a wall or a claymore on the ground showed *nothing*, to anyone. Added an **authoritative-list** render: rather than matched place/remove events (which risk ghost gadgets if any of the several removal paths — detonate, trip, owner death, bag depletion — is missed), the server rebuilds the full list from its live stores each tick and the client mirrors it. **`shared/sim/gadget_list.gd`** (`GadgetList.build`, pure/unit-tested): flattens `_c4`/`_mines`/`_bags` into one `{kind,pos,face}` list (capped at `MAX` for packet bounds) — so any placement/removal the server *already* does is reflected automatically, **with zero new server bookkeeping**. **Wire** `GADGET_LIST` (Msg 36): count + per-gadget kind/pos ×10/face ×10000; round-trip tested. **Server** `_broadcast_gadget_list` sends it **reliably on change** (+ a ~1 Hz heartbeat while non-empty so a late-joining human catches up), humans-only, skipped entirely when no human is connected. **Client/renderer**: `GADGET_LIST` → `WorldRenderer.set_gadgets` replaces the rendered set (cheap full rebuild; skipped on an unchanged heartbeat) — C4 = a dark charge with a red indicator LED, mine = a small directional claymore slab oriented by its facing, bag = a supply crate. Crosses into `shared/`+`server/` (the additive cosmetic-broadcast boundary, as `IMPACT_FX`/`GRENADE_FX`). 8 new tests (6 build + 2 codec); suite **802/0**; server boots/runs clean with a live human client (the per-tick broadcast executed without error). `--gadget-test` QA flag lays a C4/mine/bag in front of the camera; **visual-validated on .128** — all three render on the ground (C4's red LED visible). The list-authority is robust by construction; a live emergent-placement capture has the same bots-inert limitation as `GRENADE_FX` (the server emit path is still exercised — it sent the empty list to the connected human — and the pure `build` + codec are unit-tested).

#### Increment — Thrown-grenade arc (local thrower) — visual-validated ✅ 2026-06-27

Thrown frag/smoke grenades were **invisible in flight** — only the RPG rocket had a cosmetic flying object (`fire_rocket`); a thrown grenade just produced a detonation/cloud at the end with nothing arcing through the air. Added the local thrower's-eye cosmetic, mirroring the RPG pattern. **Renderer** (`world_renderer.gd`, view-only AGENTS.md §7): `throw_grenade(origin,vel,kind)` spawns a small tumbling grenade mesh (frag = dark olive, smoke = grey-green canister) and `_age_thrown` integrates it along the **shared `Grenade` ballistic model** (the same `Grenade.integrate` the server uses), vanishing on ground contact (`y<=0`) or at the **1.5 s fuse** — exactly the server's FRAG/SMOKE termination (`GRENADE_FUSE_TICKS=45`, detonate-on-ground); a smoke canister leaks a faint trail. The server's `DETONATION`/`SMOKE_DEPLOYED` then plays the blast/cloud at the landing point (no double effect). **`client_main`**: on a local frag/smoke throw it calls `throw_grenade(eye, Grenade.launch_velocity(aim_dir), kind)` — same eye-origin + launch velocity as the server, so the cosmetic arc tracks where the real grenade goes. Local-thrower only for now (remote throws would need a wire FX message, like `ROCKET_FX`); no wire/server/sim change. Rendering reuses the already-tested `Grenade` math (no new unit test, same as `fire_rocket`/`spawn_explosion`); suite **793/0**. `--grenade-test` QA flag lobs cosmetic grenades across the view; **visual-validated on .128** — a grenade arcs mid-air with a smoke trail. **+ Remote throws (2026-06-27):** completed the pair — added `GRENADE_FX` (Msg 35, origin ×10 / look dir ×10000 / kind byte, same shape as `ROCKET_FX`). Server `_broadcast_grenade_fx` (humans-only, excludes the thrower, FRAG/SMOKE only — IMPACT isn't in a human loadout) fires from `_handle_grenade_throw`; the client decodes it and calls the same `throw_grenade(origin, Grenade.launch_velocity(dir), kind)`, so other players' grenades arc too. Crosses into `shared/`+`server/` (the owner-approved additive cosmetic-broadcast boundary, as `IMPACT_FX` did). Codec round-trip test (FRAG+SMOKE); suite **794/0**, server boots/runs clean. The remote path is validated by the codec test + structural equivalence to the proven `ROCKET_FX` remote broadcast (line-for-line mirror — RPG rockets already render on remote clients) + the `throw_grenade` renderer's prior visual validation; a live emergent-bot-throw capture was impractical (16 bots/75 s stayed below engagement pressure → `nades=0`, the known bots-inert limitation — gating a cosmetic on emergent AI is the anti-pattern per AGENTS.md §10).

#### Increment — Smoke-grenade clouds — visual-validated ✅ 2026-06-27

Smoke grenades were **invisible**: the server emits `SMOKE_DEPLOYED` (msg 14, pos/radius/expire) and tracks the zone for gameplay, but the client had **zero handling** — throwing smoke deployed a tactical zone with no cloud to see. Added the cloud render. `client/smoke_cloud.gd` (`SmokeCloud.envelope(age,duration)`, pure/unit-tested): the opacity curve — billow-in over `GROW` (0.6 s), hold fully opaque, thin out over the last `FADE` (1.2 s); degrades gracefully for clouds shorter-lived than GROW+FADE (the ramps overlap). **Renderer** (`world_renderer.gd`, view-only AGENTS.md §7): `spawn_smoke(pos,radius,duration)` builds a cluster of `SMOKE_PUFFS` soft unshaded spheres on a deterministic golden-angle spiral filling the zone disc and billowing up from the ground; `_age_smokes` drives every puff's alpha by `SmokeCloud.envelope` each frame (+ a slow drift/expansion) and frees the cloud at expiry. Bounded for 128p: `SMOKE_MAX_CLOUDS` (24) caps concurrent clouds, dropping the oldest. **`client_main`** handles `SMOKE_DEPLOYED` → derives the lifetime from the server's absolute expiry tick vs `_last_server_tick` (30 Hz, clamped, 5 s fallback) → `spawn_smoke`. No wire/server/sim change. `--smoke-test` QA flag re-pops a cloud in front of the camera on a cadence (a one-shot fires at the pre-deploy origin and is left behind — the re-pop lands one in front of the *deployed* player). 6 envelope tests; suite **793/0**; **visual-validated on .128** (iGPU/Wayland) — a dense grey cloud sits on the ground downrange, obscuring the world behind it while the HUD/viewmodel stay crisp. Colour/density feel-tuning is owner-gated (AGENTS.md §10).

#### Increment — Support-action feedback beams (heal/ammo/repair/revive) — visual-validated ✅ 2026-06-28

Active support — a Medic healing, a Support resupplying, an Engineer repairing a friendly vehicle, a reviver holding a downed teammate — produced **zero** visible feedback to anyone (giver, target, or bystanders). The server tracked it all (`_giving`/`_repairing`/`_being_revived`) but never broadcast it. Added a view-only support-link visual: a coloured beam from giver to target + a pulsing aura on the target. `shared/sim/support_links.gd` (`SupportLinks.build`, pure/unit-tested): flattens the per-tick support actions into a deduped, deterministically-sorted, capped (`MAX=64`) render list. **Wire** (`Protocol.SUPPORT_LIST`, msg 37): giver_id u32 + target_id u32 + kind u8 (`HEAL`/`AMMO`/`REPAIR`/`REVIVE`) — **no coordinates**; the client resolves both endpoints from the snapshot it already mirrors (pawn ids or the disjoint vehicle-id range) and skips any it can't see. Same broadcast shape as `GADGET_LIST` (rebuilt each tick, sent on change + ~1 Hz heartbeat, skipped entirely with no human connected → 0 cost at bot scale). **Server**: the give/repair/revive steps record what they acted on into `_support_links_this_tick`; `_broadcast_support_list` sends it. **Client** (`world_renderer.gd`, view-only AGENTS.md §7): a pooled `looking_at`-oriented beam (stretched per frame, mirrors the tracer pattern) + a translucent breathing aura sphere, colour by kind (heal green / ammo amber / repair cyan / revive white-green); endpoints raised to chest, resolved via new `WorldView.local_id()` + the predicted local pawn + remotes + the vehicle pool. `--support-test` QA flag pins two camera-parented dummies linked by a live cycling beam. 8 new tests (build dedup/sort/cap + protocol round-trip); suite **851/0**; **visual-validated on .128** — a green heal beam + aura links two soldiers, the aura clearly enveloping the target's torso. No sim/gameplay change; colour/intensity feel-tuning owner-gated.

#### P2 increment — Fire-mode indicator + armor visual diffs — visual-validated ✅ 2026-06-24

Two more M5.5 → M7 presentation deferrals. Design: [`docs/superpowers/specs/2026-06-24-firemode-armor-fx-design.md`](../superpowers/specs/2026-06-24-firemode-armor-fx-design.md). Branch `m7-firemode-armor-fx`.

- **Fire-mode (client-only):** fire modes existed in the sim + wire (`SET_FIRE_MODE`) but nothing client-side selected or showed them — so the server always used the default (modes were inert end-to-end). Now `WeaponPredictor` tracks `fire_mode` (resets to the weapon default on swap), `cycle_fire_mode()`, and `step()` respects the mode (SEMI=one/press, BURST=burst_count/press, AUTO=continuous) so the local tracer matches authority. New `fire_select` input (**V**) cycles + sends `SET_FIRE_MODE`. HUD shows an `AUTO/SEMI/BURST` glyph above the ammo (hidden for RPG).
- **Armor diffs (1 immutable replicated byte):** armor tier is class-derived + immutable per life but wasn't replicated. Added `EntityState.armor_class` + `Pawn.to_state()`; `Snapshot` carries it as a **byte on ENTER records only** (no field-mask growth, zero per-tick cost; retained across CHANGED via the cached view entry). `ArmorVisual.apply()` tints the procedural soldier's torso vest (tan/olive/slate) + scales the helmet by tier — **not** team identity (friend/foe stays the blue triangle). `--armor-demo` QA flag pins LIGHT/MEDIUM/HEAVY dummies in front of the camera.
- **Validation:** full suite **726/0** (6 predictor + 2 snapshot-codec + 3 ArmorVisual tests). Visual-validated on .128 (iGPU): the three armor tiers render clearly distinct and the `AUTO` glyph shows in the HUD. GLB-character armor visual + exact shades/key are owner follow-ups (AGENTS.md §10).

#### P2 increment — Suppression screen FX — visual-validated ✅ 2026-06-24

Closes the visual half of the M5.5-P2 "suppression screen blur/shake/muffle" deferral (audio half already shipped). Design: [`docs/superpowers/specs/2026-06-24-suppression-screen-fx-design.md`](../superpowers/specs/2026-06-24-suppression-screen-fx-design.md). Branch `m7-suppression-screen-fx`.

- Driven by the **already-replicated** `SELF_STATE` own-suppression scalar (M5.5-P2) — the client already fed it to audio; this adds the matching screen effect. Client-only; no `shared/`/server/wire change.
- `HudModel.suppression_intensity()` (pure, unit-tested): zero below the 0.25 threshold (aligns with the audio onset), smoothstep to full. `HudView` full-screen canvas shader — first HUD child so it samples only the rendered world (HUD draws crisp over it) — applies a **tunnel vignette + desaturation + edge blur** scaled by a `strength` uniform; built before the flashbang white-out so a flash still covers it. `client_main` drives it each frame (alive + not-downed gating, same as the blind overlay).
- QA: `--suppress-test` mirrors `--flash-test`; with `--shot-after` it captures a same-camera **clean→suppressed A/B** for screenshot diffing.
- **Validation:** full suite **715/0**; **visual-validated on .128** (iGPU, RADV Renoir, Wayland) via the self-screenshot recipe — same-camera A/B confirms the tunnel-darken/desaturate/blur veil over the world while the HUD stays crisp, and a perfect passthrough at strength 0. Feel-tuning of intensity is an owner playtest follow-up (AGENTS.md §10). **Audio gets its own spec** (`docs/specs/audio.md` — reserved; brainstormed when P2 starts): distance-attenuated + occluded gunfire, footsteps, **bullet crack/whiz** (from M5.5 projectiles), suppressor signature, suppression muffle, explosion/vehicle, directional.

**P2 gate (the full M7 gate):** end-to-end human playtest of a full Conquest match **with the real art and complete HUD.**

#### P2 increment — Imported GLB characters (animated) — owner-validated ✅ 2026-06-18

First real art swap: the player **character** moves from procedural boxes to the **Kenney "blocky characters"** GLB (CC0), with their 27 built-in node-transform animations. Supersedes the *character* portion of the procedural-art plan (weapons/vehicles/structures/props stay procedural). Plan: [`docs/plans/2026-06-18-m7-p2-glb-characters.md`](../plans/2026-06-18-m7-p2-glb-characters.md). Branch `m7-p2-glb-characters`.

- **Architecture:** new `client/art/` presentation factories — `CharacterAnim` (pure state→clip map), `GlbCharacterKit` (load + height-normalize to STAND_HEIGHT, wrapped in an identity-scale `Node3D` so stance-scaling composes), `CharacterDriver` (idempotent clip play). Behind a persisted `ClientSettings.use_model_characters` flag (default **false** → procedural fallback intact). Renderer seam: `world_renderer._make_entity_mesh()` + `_pose_entity()`. No `shared/`/server/bot changes — client-only.
- **v1 animation:** idle / walk / sprint (from a per-frame speed estimate), crouch = vertical shrink, prone = face-down tip, **downed (DBNO) = face-up on the back + calm idle** (playtest-driven fix; the `die` clip read as "hands-up").
- **Validation:** full unit suite **465/0**, ≤48-bot smoke PASS (peak 19.45 ms), spec + code-quality two-stage review on the renderer integration. **Owner playtest 2026-06-18** on the home laptop (.128, RADV Renoir/Vulkan) → game2 server+bots: "looks much better… good enough for now"; crouch/downed/controls confirmed.
- **Follow-ups (tracked, not blocking):**
  - **Settings file clobbered by the test suite — FIXED 2026-06-18.** Real root cause was *not* the in-game menu (it preserves the flag): `SettingsMenu.apply()` saved via `ClientSettings.save_to()`'s default path, so `tests/settings_menu_test.gd` overwrote the real `user://settings.cfg` (all fields, incl. `use_model_characters→false`) on every `--test` run. Fixed by injecting `SettingsMenu.save_path` (tests use a temp file) + a regression test. *Separately observed, not yet addressed:* the menu may show default control values if `bind_settings` runs before `_ready`, which would reset sensitivity/FOV/volume on apply (distinct from the flag); and the flag still has no in-game toggle (file-only).
  - Part 2 refinements per the plan: authored crouch/prone poses; remote **fire** anim (needs `shooter_id` on `SHOT_FX`); reload/jump (need new signals); optional team/squad variants.

## Rendering backend
**Forward+ (Vulkan)** primary with **GL Compatibility** fallback for old hardware — [ADR-0005](../adr/0005-client-renderer.md). Client-only; server/bot stay headless.

## Run topology (playtest)
Client on the owner's **desktop** (display + GPU), dedicated server + bots **headless on game2**, connected over **LAN** (`--connect=<game2-ip>`) — the normal client/server split, sub-ms latency. The agent builds + runs the deterministic tests and the headless server on game2; the owner is the renderer at each checkpoint.

## Testing discipline (AGENTS.md §10)
Deterministic headless tests for everything testable — prediction/reconciliation, ammo prediction, vehicle prediction, HUD-model logic, the new netcode messages, interpolation/view logic. Human playtest for the rest — movement/gunplay feel, reconciliation smoothness, vehicle handling, HUD readability/layout, art. Don't tune feel blind.

## Specs
- [client-prediction.md](../specs/client-prediction.md) — P1 client architecture, prediction/reconciliation, new netcode, test plan. ✅
- [hud-ui.md](../specs/hud-ui.md) — P1 HUD model+view, menus, keybind defaults, test plan. ✅
- [ADR-0005](../adr/0005-client-renderer.md) — renderer choice. ✅
- [art-pipeline.md](../specs/art-pipeline.md) — **P2** presentation design-of-record (kit conventions + GLB import flow + open tracks: LOD, animation, combat VFX). Drafted 2026-06-18; structure-destruction visuals scoped out (owned by M11). ✅

## Deferred out of M7
- **Steam auth + VAC** (Layer 5) and **anti-cheat L3 — LOS replication culling** (Layer 3) → later online/anti-cheat track; see [anti-cheat-matchmaking](../specs/anti-cheat-matchmaking.md).
