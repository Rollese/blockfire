# M12 — Squad FOB, Cooperative Construction & Class Refit

**Status:** P1 (Class refit) **CLOSED ✅** · P2 (Cooperative shovel construction) **CLOSED ✅ — 128-bot fleet gate PASS 2026-06-28** · P3 (Squad-leader FOB) **CLOSED ✅ — 128-bot fleet gate PASS 2026-06-29** — **M12 COMPLETE** · **Sequence:** near-term — **lands before/alongside the in-progress M7 client** (so M7 builds the right class-select, shovel/build, and deploy/spawn UI) · **Decision:** [ADR-0007](../adr/0007-battlebit-divergences.md) · **Spec:** [`squad-fob-class-refit`](../specs/squad-fob-class-refit.md) · **Plans:** [`p1-class-refit`](../archive/plans/2026-06-18-m12-p1-class-refit.md) · [`p2-shovel-construction`](../archive/plans/2026-06-28-m12-p2-shovel-construction.md) · [`p3-fob`](../archive/plans/2026-06-29-m12-p3-fob.md)

> **M7 CLIENT — build-PLACEMENT UI landed 2026-06-30** (branch `m12-client-build-placement`; suite **911/0**; validated end-to-end on game2 via Xvfb). The BattleBit "build-tool mode" (owner-selected): **T** toggles build mode; the wheel cycles the piece (sandbag/wall/heavy_barricade, + FOB for leaders), **R** rotates 90°, **LMB** on a valid in-range ground cell sends `BUILD_REQUEST` (or `PLACE_FOB`), and holding LMB on an under-construction site sets `BTN_SHOVEL` so the server builds it. A green/red "hologram" tracks the aimed cell (`StructureStore.BUILD_RANGE`-gated so green ⇒ server accepts); fire/reload are masked in build mode; build mode never survives death/downed/vehicle/menu. Pure `client/build_controller.gd` (8 unit tests) + `WorldRenderer.set_build_preview` + a bottom HUD hint. `--build-test` auto-places + shovels a sandbag (server `bld=1`, amber piece rises). Leader is approximated as lowest-id-in-squad (server authoritatively gates `PLACE_FOB`). **Remaining M12 client gap: the 4-class select UI.**
>
> **M7 CLIENT — build-site/FOB render + deploy landed 2026-06-30** (merged + pushed to master, tip `d76524d`; suite **903/0**; visual-validated on game2 via Xvfb/OpenGL3). The sim already replicated everything; the client now consumes it: **(1)** `WorldView` handles `STRUCTURE_DELTA` `OP_PROGRESS` (was dropped) and `WorldRenderer` renders `under_construction` pieces as individual "build sites" — the real target geometry, amber-skinned, **vertically scaled to `build_progress/build_cost` so it rises from the ground** as it's shovelled (split out of the static MultiMesh batch). **(2)** Registered the two missing piece types **22=heavy_barricade, 23=fob** in the renderer type tables (they fell back to "wall"); `StructureKit` gained a thick barricade + a **low sandbag-bunker FOB placeholder model**. **(3)** `client_main` handles `Msg.FOB_LIST` (was dropped) and surfaces an **enabled squad FOB as a deploy-screen spawn** ("Squad FOB (N)") via `DeployMenu.populate(..., fobs)`. QA flags `--buildsite-test` / `--fob-menu-test`. **STILL DEFERRED → next M12 client increment: the interactive build-PLACEMENT UI** — the client never sends `BUILD_REQUEST`/`PLACE_FOB`/shovel input, so a player can render+spawn-on others' builds but cannot place/shovel their own yet. Also pending: 4-class select UI.
>
> **P3 — Squad-leader FOB: 128-bot fleet gate PASS 2026-06-29** (agent-run on game2, P-cores 0-3, `conquest_proving_grounds`, `docker/run-m12-p3-gate.sh`). Authoritative run (final merged tree): `winner=0 t0=44 t1=0 elapsed=256s cap_events=5 peak tick=22.48ms<33.3 agg=13.7Mbit/s`, **0 script errors**; FOB counters `fobs_built=1 fobs_destroyed=1 fob_disabled=0 fob_spawns=0` — `docker/srvlog-20260629-235035.log`. **Hard-asserted `fobs_built≥1`** — a squad's FOB site **cooperatively shovelled to completion by ≥2 bots live at 128, within budget** — and here a built FOB was **destroyed in-match** (`fobs_destroyed=1`). A prior run on the pre-cleanup tree showed `fobs_built=1 fob_disabled=1` (an enemy entered the FOB's 40 m vicinity and proximity-disabled its spawn; `docker/srvlog-20260629-234249.log`) — so **across the two gate runs, three of the four FOB mechanics (built / disabled / destroyed) fired emergently**. `fob_spawns` stays 0 (the FOB builds near base and is rarely the nearest spawn to the forward objective) — **reported, not hard-gated**, mirroring the M12-P2 precedent; **all four mechanics are proven deterministically** in `tests/fob_functional_test.gd` (placement/leader-only/cooperative-build/spawn-enable-boundary/destroyed-not-a-spawn/decay). Branch `m12-p3-fob` (Tasks 1-8 + review-nit cleanup); full unit suite **898/0**. **M12-P3 CLOSED → M12 COMPLETE.**
>
> **P3 design as built:** the FOB is a special **large catalog piece** (`fob`, `min_builders=2`, `health=2500`, `build_cost=800`) built through the **M12-P2 build-site machinery** — `PLACE_FOB` (leader-only) creates a FOB build site that the squad shovels to completion; on completion it becomes a normal **M4-destructible** structure. Spawn integration: `SpawnSelect.choose()` offers the squad's FOB as a source **iff completed + not-destroyed + no enemy within `Fob.VICINITY_RADIUS` (40 m)**; squadmate-rally retained as fallback. The FOB's full lifecycle (built / destroyed via any M4 removal path / pre-completion decay) is detected by a single per-tick **`_reconcile_fobs()`** pass against the live stores — robust without hooking each removal site. Replication for the M7 client: `Msg.FOB_LIST` (own-team `{squad, structure_id, under_construction, enabled}`, sent to humans only) + `DeploySpawn.FOB_BASE` deploy ref. Pure rules in `shared/sim/fob.gd`. **Client FOB UI / build-site ghost / bunker model deferred to the M7 client** (gate is headless; bots don't render). Bot drill: squad-leader bots build their own FOB at the per-team shared near-base cell (leader inference = lowest visible same-squad id), large-drillers add builders.

> **P1 — Class refit: 128-bot fleet gate PASS 2026-06-28** (agent-run on game2, P-cores 0-3). `conquest_proving_grounds`, `docker/run-m4.5-p2-gate.sh` (the combat-depth fleet exercises the Engineer gadget split): `winner=1 t0=0 t1=25 elapsed=345s<900 (ticket exhaustion) cap_events=4 peak tick=28.47ms<33.3`; gadget counters `c4=15 mines=3 rockets=6 heals=181 ammo=21 bags=30`. **Every M12-P1 criterion met:** no Recon class + DMR Assault-only (unit-proven on master), and **both Engineer Gadget-A options fire at scale — C4 (`c4=15`) AND claymore (`mines=3`)** — confirming the claymore→Engineer pick-one reassignment works emergently in a full 128-bot match within the tick budget. Evidence `docker/srvlog-20260628-184043.log`. **M12-P1 CLOSED.** (Owner may re-run for their own sign-off; mechanic also proven deterministically — see unit suite.)

> **P1 — Class refit: implemented 2026-06-18** (branch `worktree-battlebit-divergence-design`, commits `d6d5819` + `83cbf57`). Recon removed (4 classes); DMR Assault-only (`can_equip`); claymore→Engineer via a deterministic per-player gadget (`Loadout.gadget_for_player(cls, id)` — server + bots agree with no replication, so **no protocol change**). **Full unit suite 495/0**; project imports clean; no `RECON` class refs remain. **≤48-bot combat-depth smoke** (`ci/m4.5_p2_test.sh`): match reaches a winner; **C4 fires live via the new gate** (`c4=2`/`5` across runs — engineers placing C4 by id parity, ~half the pre-refit count since the other half now carry the claymore); `heals`/`ammo`/`bags`/`pen` all fire; tick ~18 ms. **`rockets` and `mines` are NOT gated at 48 bots** — post-M5 the RPG only fires at *vehicles* (engineers rarely encounter one at 48-bot density; the pre-change base barely cleared at `rockets=1`), and claymore *trips* need a point-blank enemy; both are density-dependent emergent counters the script already treats as report-only. **Authoritative P1 gate = the 128-bot fleet run on game2**: asserts no Recon, DMR Assault-only (unit-validated), C4 + claymore both fire, peak tick < 33.3 ms. **RUN 2026-06-28 → PASS** (see status line above: `winner=1 peak tick=28.47ms`, **`c4=15` + `mines=3` at 128 bots** — at full density the claymore trips fire emergently, unlike the 48-bot smoke). **M12-P1 CLOSED.**

> **Numbering vs sequence:** the milestone *number* is M12 (next free after the in-flight M11 destructible-buildings), but its *position in the build sequence is near-term* — it should land before the M7 client locks its class/build/spawn UI. Numbering ≠ strict order here (cf. M7 pulled before M6, M10 deferred last).

**Objective:** Apply the ADR-0007 §1+§2 design changes at the sim layer: remove the Recon class (DMR Assault-only, no sniper rifles; claymore→Engineer); replace M4's instant placement with **universal shovel-based progressive construction** (small pieces solo-buildable, large structures + FOB need ≥2 squadmates shovelling together); and add a **squad-leader FOB** — a **cooperatively-built, destructible bunker** that becomes a squad forward-spawn (proximity-disabled while enemies are near, destroyed via M4 destruction, hunted by enemy AI), with squadmate-rally spawn kept as a fallback. Server-authoritative, shared rules in `shared/sim/`, bot-fleet-gated + deterministically proven (the M3/M4/M4.5 mould).

## Phases (each independently gated)

- **M12-P1 — Class refit.** 4 classes (Recon removed); DMR Assault-only; claymore→Engineer (pick-one vs C4). Self-contained, no new systems, **independent of M11** — lands first.
- **M12-P2 — Cooperative shovel construction. CLOSED ✅ 2026-06-28** (branch `m12-p2-shovel-construction`, plan [`2026-06-28-m12-p2-shovel-construction`](../archive/plans/2026-06-28-m12-p2-shovel-construction.md)). Universal shovel via the held `BTN_SHOVEL` bit; `BUILD_REQUEST` now creates a *build site* (`BuildSiteStore`, no collision until complete) that accrues progress as eligible builders shovel it (`BuildSite` pure math); min-builders scale by size (small=1, large=2 via `heavy_barricade`); completion → `StructureStore.place` (a normal M4 structure); shovel-repair (friendly, `StructureStore.repair_chunks`); **enemy shovel-dismantle** (the M4 melee path); abandoned-site decay; replication via `under_construction`/`build_progress` record fields + `OP_PROGRESS`. **Mechanic proven deterministically** (`tests/build_construction_functional_test.gd`: solo-small / blocked-solo / built-by-two / decay / dismantle / repair — all green; suite 878/0). **128-bot fleet gate PASS** on game2 (`conquest_proving_grounds`, `docker/run-m12-p2-gate.sh`): `winner=0 t0=17 elapsed=312s<900 cap_events=4 peak tick=24.29ms<33.3 built_large=1` (a heavy_barricade cooperatively shovelled to completion by ≥2 bots, live, within budget) — `docker/srvlog-20260628-225159.log`. `built_small`/`bsolo`/`dismantled`/`repaired` are emergent/density-variant at 128 (AGENTS.md §10; reliable at ≤64 bots) and reported, not hard-gated — all unit-proven. **Supersedes M4 instant placement**; reuses M4 grid/catalog/collision + M4 destruction. **Client ghost-rendering of in-progress sites deferred to the M7 client** (gate is headless; bots don't render). **P3 (FOB) not started.**
- **M12-P3 — Squad-leader FOB. CLOSED ✅ 2026-06-29** (branch `m12-p3-fob`, plan [`2026-06-29-m12-p3-fob`](../archive/plans/2026-06-29-m12-p3-fob.md)). Leader places a FOB build site (one per squad, `Msg.PLACE_FOB`); squad shovels it (≥2, via the P2 path) into a high-HP (`Fob.HEALTH=2500`) M4-destructible bunker; becomes a spawn source gated by **completed + not-destroyed + no enemy within `Fob.VICINITY_RADIUS`=40 m** (`SpawnSelect.choose` source kind); squadmate-rally fallback retained. Pure rules `shared/sim/fob.gd`; server FOB registry + `_reconcile_fobs()` lifecycle detection; `Msg.FOB_LIST` + `DeploySpawn.FOB_BASE` replication for M7. **128-bot fleet gate PASS** (`fobs_built=1 fobs_destroyed=1` peak tick 22.48ms; `docker/srvlog-20260629-235035.log`); mechanics proven deterministically (`tests/fob_functional_test.gd`). **Client FOB UI / build-site ghost / bunker model deferred to M7.**

## Scope

- **Class refit** (`shared/sim/loadout.gd`, `weapon.gd`): 4 classes; DMR Assault-only (`can_equip`); no sniper weapon added (DMR stays semi-auto); Engineer Gadget A = C4 *or* claymore.
- **Shovel construction** (`shared/sim/build_site.gd` NEW, `structure.gd`/`piece_catalog.gd` ext): build sites with `build_progress`/`min_builders`; per-tick progress from eligible shovellers (interest-grid bounded); completion→structure; friendly repair; **enemy shovel-dismantle** (reduces site progress / structure HP via the M4 removal path); decay; catalog gains `build_cost`/`min_builders` + ≥1 large piece.
- **FOB** (`shared/sim/fob.gd` NEW + spawn path): leader-placed large build site → destructible bunker; one per squad; persists through leader death; proximity-disable; M4-destruction-removable.
- **Spawn selection** (`server/server_main.gd`): source set = home base + owned points + FOB (completed+enabled+alive) + alive squadmate (fallback).
- **Protocol** (`shared/net/protocol.gd`): `PLACE_FOB`/`REMOVE_FOB`; shovel-use input bit; `BUILD_REQUEST`→site; `STRUCTURE_DELTA` + `build_progress`/`under_construction`; FOB entity replication. **Sequenced after M11 merges** (shared structure store / protocol).
- **Bot AI** (`bots/bot_driver.gd`): 4-class rolls; shovel-build cover; **squad bots cooperatively build the FOB** (leader places, ≥2 shovel); enemy bots target/destroy enemy FOBs (**shovel-dismantle fallback when out of explosives**); scripted squad-build drill exerciser.

## Gate

Per phase, holding tick + bandwidth budget (mean tick < 33.3 ms at 128) and Conquest still reaching a winner; mechanics proven deterministically (AGENTS.md §10):

- **P1:** No Recon class present; DMR equips **only** on Assault (loadout-validation unit test rejects DMR elsewhere); claymore selectable on Engineer.
- **P2:** A **small** piece builds with **1** shoveller; a **large** piece advances **only with ≥2** simultaneous shovellers (telemetry: `built_small`, `built_large`, `build_blocked_solo`); shovel-repair restores HP; **enemy shovel-dismantle** removes an enemy structure (`dismantled ≥ 1`); budget held under 128-bot building load.
- **P3:** A squad **builds a FOB cooperatively** (`fobs_built ≥ 1`), **spawns on it** (`fob_spawns ≥ 1`), has it **proximity-disabled** by enemies (`fob_disabled ≥ 1`), and **destroyed** (`fobs_destroyed ≥ 1`) — all observed in a 128-bot match; squadmate-rally fallback still functions; a winner declared via tickets.

Deterministic proof (unit/integration), not gated on emergent AI: loadout restrictions; build-site progress + min-builders boundary; FOB placement validation, enemy-proximity enable/disable boundary, destroyed-FOB-not-a-spawn; spawn-source selection.

## Specs

- [`docs/specs/squad-fob-class-refit.md`](../specs/squad-fob-class-refit.md) — brainstormed; design approved 2026-06-18.

## Supersedes / touches prior milestones

- **M3** "spawn on any alive squadmate" primary → superseded by the leader FOB (squadmate spawn demoted to fallback). M3 doc carries a superseded note → ADR-0007.
- **M4 (Building)** instant placement → superseded by progressive shovel construction (build sites). **M4 destruction** + grid/catalog/replication are reused unchanged. M4 doc carries a superseded note → ADR-0007.
- **M4.5** Recon class + Recon-owned claymore → Recon removed; claymore reassigned to Engineer. M4.5 doc carries a superseded note → ADR-0007.

## Cross-milestone notes

- **M7 (client):** consumes this — build the 4-class select, the shovel/build UI + build-site ghost, and the FOB deploy/spawn UI + placeholder bunker model. Land M12's sim change before M7 finalizes those screens.
- **M11 (destructible buildings, in-flight):** shares `shared/net/protocol.gd` **and the structure store / `STRUCTURE_DELTA`**. M12-P2/P3 build on the same machinery — sequence them after M11 merges or coordinate with that agent. **M12-P1 (class refit) is independent of M11 and can land first.**
