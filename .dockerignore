# Keep the build context lean and platform-clean.
.git
.godot          # regenerated by `godot --import` during the image build (host artifacts are platform-specific)
graphify-out
**/*.log
docs
ci
*.md
.gitignore
